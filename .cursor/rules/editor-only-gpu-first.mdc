---
description: 项目核心约束：仅编辑器单实例模式运行 + GPU 优先
globs: **/*.gd, **/*.glsl
alwaysApply: true
---

# MeshFill-Godot 项目规范

## 1. 仅 -e 编辑器模式运行

本项目禁止以游戏模式 (F5 / `godot --path`) 运行，只允许编辑器模式 (`godot -e`)。

- 编辑器单实例由两层守卫保证，**禁止删除或绕过**：(1) 所有场景树脚本通过 `@tool` + `if not Engine.is_editor_hint(): return` 守卫，确保运行时回调只在编辑器执行；(2) `non_headless_scene_guard.gd`（挂在 `demos/common_demo_setup.tscn`，由 `core_demo_contract_fixture.gd` 调用）在检测到 headless 显示后端时 `push_error` + `quit(1)` 拒绝场景启动
- 启动 `godot -e` 前必须确认当前项目没有已运行的 Godot 编辑器实例；同一项目一次只允许一个编辑器实例，发现已有实例时复用或关闭原实例，禁止再启动第二个
- 所有挂载到场景树的脚本（extends Node / Node3D / Camera3D 等）如果包含 `_ready()` / `_process()` / `_physics_process()` / `_unhandled_input()` 等回调，**必须添加 `@tool` 注解**，否则编辑器模式下回调不会执行
- 不需要编辑器回调的纯数据脚本（extends Resource / RefCounted）无需 `@tool`
- EditorPlugin 脚本天生在编辑器运行，已有 `@tool` 即可
- 新建 .gd 文件时，若 extends Node 系且有运行时回调，默认加 `@tool`

## 2. GPU 优先原则

所有体素、高度图、法线图、统计等数据处理逻辑，**必须优先使用 GPU Compute Shader 路径**。

- 当同一功能同时存在 CPU 和 GPU 实现时（如 `decode_target_read_buffers` vs `decode_target_read_buffers_gpu`），**必须调用 GPU 版本**
- CPU 回退仅在 `RenderingDevice` 不可用时作为 fallback，且需打印警告
- 新增数据处理逻辑时，优先编写 GLSL Compute Shader + `GodotComputeShaderBase` 管线
- 禁止在 GDScript 中用 for 循环逐体素迭代大规模缓冲区（texture_size * texture_size * slice_count 量级），改用 Compute Shader dispatch
- GPU buffer 创建使用 `GodotComputeShaderBase` 提供的 `storage_buffer_from_bytes()` / `storage_buffer_zero()` 等工具方法

## 3. 测试场景规范

- 每个功能模块的测试 `.tscn` 场景必须放在对应的 `demos/<模块名>/` 文件夹下
- 命名格式：`demos/<模块名>/<模块名>.tscn`
- 测试脚本（`.gd`）与测试场景放在同一目录
- 外部测试工具（如 MCP 驱动的 PowerShell 测试）放在 `tools/` 下
- demo 文件夹同时包含 `.md` 文档说明测试方法与验收标准

## 4. 项目约定

- 地形比例真值源：`TerrainConfig`（`terrain_config.gd`）
- Compute Shader 基类：`GodotComputeShaderBase`（`godot_compute_shader_base.gd`）
- GPU 对象运行时：`GPUAutoObjectRuntime`（`gpu_autoobject_runtime.gd`）
- TargetSV 加载器必须使用 GPU 解码路径
