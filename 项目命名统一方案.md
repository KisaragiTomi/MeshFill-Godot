# 项目命名统一方案

2026-07-10 命名审计（8 概念轴 + 2 交叉核对，61 个聚类逐条核实）的结论与统一词汇表。核心发现：**表面上的"别名"多数是管线阶段名**——同一份资产体素数据每过一个阶段都发生真实变化（归一化、增补、派生、截断），扁平化成一个名字会抹掉真实区别；正确解法是**词根 + 阶段限定词**。真正的同义词、误导名、假朋友（同名异义）各有一批，按成本分层列出。

审计基线 = 当日 `refactor/compute-pass-toolkit` 工作区；`voxel_placement_output.gd` 等文件当日有并发重构（已瘦身 + codec 外提），执行前按当前树复核行号。

## 主线：资产体素表示的六个阶段（你问的 profile/footprint/write_spec 链）

| 阶段 | 数据实质 | 规范名 | 说明 |
| --- | --- | --- | --- |
| 授权采样 | `Array[Dictionary]` 局部体素采样 {pos, collision_strength, flags, weight} | `collision` | 契约冻结键；文档明令禁止旧别名 `collision_voxels` |
| 导入期资源 | `{color, complexity, collision}` 的 Resource 包装 | `AutoVoxelProfile` / `voxel_profile` | `.tres` 序列化属性名，冻结 |
| 运行时注册 | SPA 归一化 + hash 去重 + **增补 pivots/probes** + GPU 上传（profile_table） | runtime profile（`profile_id`/`profile_hash`） | 与导入期是两个阶段，非同义 |
| 评分/盖章形状 | `bake_footprint(collision)`：过滤 + **合成 clearance 体素** + 去重 + 截断 128 | `footprint` | 喂 score 与 stamp 的同一组 GPU buffer |
| 每实例写记录 | 共享字段 + 实例变换 + base_pixel/channel 等写参数 | `instance_stamp_write_spec`（ISWS） | **唯一真同义对**：`voxel_write_spec` ≡ ISWS，文档已裁定 ISWS 为 canonical，代码滞后 |
| 提交写 | GPU scatter（stamp_voxel_field）/ CPU committer 路径 | `stamp` | "stamp-only commit" 是项目既定提交模型 |

**行动**：此链唯一要做的改名 = 完成 ISWS 迁移（`make_*_voxel_write_spec` 与 `voxel_write_spec` 属性 → `instance_stamp_write_spec` 家族，约 15 文件 + 约 10 处文档；legacy 元数据键按文档约定保留为兼容别名）。其余五个阶段名**保留**，在文档中固化阶段语义。

> ✅ **已完成 2026-07-11（commit `e808ef7`）**：15 文件（9 代码 + 6 文档）。canonical=`instance_stamp_write_spec` 单一名；legacy 方法名（`make_/set_/get_/apply_voxel_write_spec`）全删、全调用方 + 鸭子类型探测点同 commit 更新；写侧单键（只写 `INSTANCE_STAMP_WRITE_SPEC_META_KEY`），`voxel_write_spec` 仅保留为读兼容 meta-key alias（+ ingest config-key alias）。`-e` 验证通过、桥应答。

## 全局命名法则（词根保留表）

| 词根 | 保留给 | 禁止用法 |
| --- | --- | --- |
| `volume` / `tile` / `voxel` | 整个体素缓冲 / 8³ 体素块 / 单元格 | "voxel region"/"voxel sparse" 指 tile（改 `candidate_tile_*`）；`collision_cell_count` 的 cell（改 voxel） |
| `profile` | 资产语义家族（authored / runtime 两阶段） | prefilter 的 "route profile"（改 `route_extent`）；`SemanticProbeProfile` 全限定可保留 |
| `anchor` / `pivot` | 场景侧候选格 / 资产侧挂接点偏移 | 二者互换；AssetDescriptor 两处 "anchor/pivot" 注释混用（改 pivot） |
| `field` | 稠密逐体素通道缓冲（505 vs 15 压倒性） | shader 局部的 "*Volume" 同义用法；committer 私有 `_volume` 字典（改 `_volume_state`） |
| `stamp` | 提交写路径专用 | `stamp_brush_sv_records`（BrushSV 是明示非提交叠加层，改 `write_brush_sv_records`）；brush 的画笔范围（`_accumulate_footprint` 改 `_accumulate_brush_extent`） |
| `summary` | 每 tile 的 32 字节 GPU 聚合记录（数据面） | 状态摘要 dict（新代码用 `status`/`report`；`get_*_gpu_buffer_summary` 报告面已冻结不动） |
| `debug` | 可关闭的观测数据（Debug 方案 diag 层） | 阻断性守卫（`score_contract_debug` 实为 contract guard，DebugBufferSet 分实例时更名 `*_contract_stats` vs `*_stats`） |
| `snapshot` | GPU readback 组装结果（`readback_*_snapshot`） | 纯 CPU dict 拷贝（`_dirty_*_snapshot` 改 `_copy`）；`readback_sv_field_debug_projection` 的 projection（改 snapshot） |
| 动词方向 | `upload`=CPU→GPU；`readback`=GPU→CPU；`writeback`=accepted-placement 传播阶段；`flush`=清排队 | `_write_bulk_spawn_records`（实为 upload） |
| 字节动词 | `pack_*`=分配并返回；`encode_*`=写入既有偏移；`decode_*`=字节→值（BufferUtils 现行分工，固化为法则） | — |
| 初始化动词 | `init_`=构造期；`setup_`=一次性接线；`ensure_`=幂等惰性保证；`prepare_`=每次运行装配 | `_prepare_gpu_runtime_for_scene_voxel_committer` 实为 ensure |
| 报告动词 | `get_*_summary`=廉价 CPU 元数据；`readback_*`=显式 GPU 读回；诊断读回开关统一前缀 `debug_read_<subject>` | `get_object_summary`/`get_debug_summary` 实际做 GPU 读回（应入 readback 层） |
| `sv` 缩写 | 标识符内允许，但**必须下划线分隔**（`target_sv`/`blend_sv`/`brush_sv`） | 新代码禁用挤压式 `targetsv`/`blendsv`/`svtile`（旧冻结面保留） |
| 生命周期 | `placement`=打分位姿 → `accepted placement` → `spawn`=GPU 常驻记录 → （节点化用 spawn 词根） | `instantiate` 只留给 `PackedScene.instantiate` |

## 分层改名清单

### A 层：低成本高价值（无契约面，建议一批做完）

| 改名 | 范围 |
| --- | --- |
| GLSL 局部 `pose` → `origin_score`（row0 = origin.xyz + score.w，无朝向，名字误导） | 5 个 shader，仅局部变量 |
| `target_color_rgba8` → `target_visual_rgba8` 并**删除重复 buffer 写入**（bake shader 同值写两个 SSBO） | ~68 处 / 8 文件，顺带省一次全量写 |
| SPA `_resident_target_color_rgba8_buffer` → `_resident_target_field_buffer`；RID 只发布一个键 | SPA + VPG ~70 处 |
| GLSL `TARGET_STATS_MAX_OCCUPANCY` → `_MAX_COMPLETELY`（与 GD 侧同槽异名） | 3 shader + 1 脚本 |
| tile summary GLSL 成员 `summary[]` → `tile_summaries[]` 统一；`collision_cell_count` → `collision_voxel_count` | 3 shader + ~17 处 GD |
| stride 双键坍缩（`*_stride_bytes` 载荷键删除，留测试钉住的 `resident_route_*_stride`） | prefilter 2 行 + SPA 3 行 |
| `instance_id` 四键合一（`auto_instance_id`/`instance_mesh_id`/`mesh_instance_id` 停写，读链保留） | 2 文件 ~8 写点 |
| `auto_object_id` 双写删除（canonical=`record_id`，`auto_id` 只留节点属性） | 3 文件 ~10 写点 |
| `auto_source` 收编 `placement_source`/`source` 摄入别名（读链保留一版） | auto_object.gd 2 处 |
| `no_voxel_regions`/`no_valid_voxel_regions` → `no_candidate_tiles`/`no_valid_candidate_tiles`（已核实无任何测试断言） | 2 处 |
| `stamp_brush_sv_records` → `write_brush_sv_records`；brush `_accumulate_footprint` → `_accumulate_brush_extent` | SPA 1 方法 + spa_test 调用；brush 2 函数 |
| `_write_bulk_spawn_records` → `_upload_bulk_spawn_records`；`_prepare_gpu_runtime_...` → `_ensure_gpu_runtime_...` | 各 1 文件私有 |
| `readback_sv_field_debug_projection` → `..._snapshot`；`_dirty_*_snapshot` → `_copy` | 各 ~5 调用点（前者含 test_markdown_contracts:239 同步改） |
| route "profile" → `route_extent`（prefilter 内部） | 1 脚本 |
| 注释级：AssetDescriptor 两处 anchor/pivot 混用、`completeness` 拼写飘移、`target_sv_pack_read_buffers.glsl:4` 的 "readback" 误用、TileTopKBuffer 等注释名对齐块名 | 零行为 |

### B 层：中成本，逐项单批（含契约面或串派发，全有或全无）

| 改名 | 代价与约束 |
| --- | --- |
| ✅ ~~ISWS 完成迁移（主线行动）~~ **已完成 commit `e808ef7`** | ~15 文件 + ~10 文档；`get_voxel_write_spec` 有鸭子类型探测点 |
| `voxel_sparse` 家族 → `candidate_tile_ids`/`asset_tile_votes`（GLSL 成员 + GD 打包侧锁步） | ~5 文件锁步改；文档契约键 `candidate_voxel_regions_by_asset` 冻结不动 |
| ✅ ~~GPU 对象属性键统一 `object_type`/`object_flags`（删裸 `"type"`/`"flags"` + 别名表）~~ **已完成 commit `7d12611`** | runtime+VPG+SPA+3 shader ~120 处；假朋友(collision flags / mesh-desc flags / command type / SV type)已甄别保留；GPU_BUFFER registry const + get_gpu_buffer 别名表 + object_flags 读回兜底 + record 键均已统一 |
| `scene_complexity`/`scene_minmax` 等 → `complexity` 词根（"scene" 让位给 SV 结构名） | ~50 处 GD + GLSL 注释 |
| `tile_id`(String "x:y:z") → `tile_key`；int 一律 `tile_index`（假朋友，一半一半，禁机械替换） | ~47 处 store + 门面 + 2 demo |
| `get_scene_voxel_tile_gpu_buffer_summary` → `..._status`（与数据面 summary 破词撞） | ~28 处含串派发，全有或全无 |
| ✅ ~~诊断读回开关统一 `debug_read_<subject>`~~ **已完成**（字符串键 commit `75825f7` B-2.7；残余本地变量 `read_debug_voxel`/`read_debug_snapshot` commit `be5b9e5`） | ~60 处字符串键；`readback_stats`→`debug_read_stats` 已同 spa_test:633 改毕 |
| `asset_id`(int) → `asset_index`（String 资源身份独占 `asset_id`） | ~30 处 SPA 公共 API + 调用方，**需拍板** |
| ✅ ~~AutoObject `voxel_descriptor` 属性 → `asset_descriptor`~~ **已完成 commit `167845c`** | 84 处；已加 `_set`/`_get` 序列化 shim(现树无序列化 voxel_descriptor，shim 为外部/旧场景安全网)；无 false-friend、无桥依赖 |

### 冻结名单（禁止机械改名）

- **契约测试逐字断言**：`candidate_voxel_regions_by_asset`（文档契约 + 禁语列表机制）、`collision` 键、`instance_stamp_write_spec`、`missing_auto_voxel_profile_buffers`、`stamp_only_commit`（spa_test:729 断言 mode 字符串）、`resident_route_record_stride`/`range_stride`。
- **序列化面**：`.tres` 里的 `voxel_profile`、`asset_id`、`pivot_variants` 等 @export 属性名（改名破坏资源加载）。
- **桥客户端**：`tools/spa_click_test.py` 硬编码 `VD_KEYS = ["gpu_objects","svtile","anchor","sv","targetsv"]` 及串派发方法名（含 `refresh_volume_score_anchor_selection`）；`tools/editor_bridge_probe.js` 同类。
- **GLSL↔GD 共享 schema 词汇**：`footprint_pos_strength`/`footprint_weight_flags` 等跨语言成员名；shader 文件名（被 md 契约与全目录编译测试引用）。
- **既定不动**：`candidate_route_*`/`resident_route_*` 家族（650+ 处，方案即"保留阶段限定词"）；`target_read_buffers`（133 处 settings 键）；~~`completely` 词根~~ **已拍板改 `completeness`（commit `3083d60`）**。

## 迁移纪律

- 每项 = 一个外科式提交；串派发/鸭子类型方法改名必须与全部调用方 + 测试**同 commit**（全有或全无）。
- GLSL 成员改名单 shader 内原子编译，但与 GD 打包端锁步改；std430 布局不受名字影响，字节契约零风险。
- 读链兼容别名（`get(new, get(old, ...))`）保留一个迁移期，写侧立即单键。
- 验证按 CLAUDE.md：`-e` 启动 + 6800 桥应答；涉及点击/显示键的项加 `spa_click_test.py` 桥用例。
- 批量执行按 ≤32 subagent 分批委托；执行前先按当前工作区复核（本文档行号为审计基线）。

## Open Questions

- ✅ ~~`completely` vs `completeness`~~ **已拍板一次性纠正为 `completeness`（commit `3083d60`，22 文件；2 个 diagram/doc 标签因载有既存 WIP 暂缓，随该 WIP 一并提交）**。
- ✅ ~~`asset_id`(int)→`asset_index`~~ **已完成（commit `3261a20`，OQ-2）**。
- ✅ ~~`voxel_descriptor`→`asset_descriptor`~~ **已拍板并完成（commit `167845c`，OQ-3/B-9；14 文件 + `_set`/`_get` shim）**。
- DebugBufferSet 落地时 `score_contract_debug`/`candidate_route_*_debug` 按"contract vs stats"分实例更名（与《统一Debug承载与输出字典精简方案》联动执行）。
