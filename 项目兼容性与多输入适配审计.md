# 项目兼容性与多输入适配审计

本文按当前工作区快照梳理兼容性代码和多种输入参数适配代码，为后续全项目优化提供删除、收敛和保留依据。审计只新增本文，不修改现有源码、资源或测试。

## 审计口径

- 文件集合来自 `git ls-files --cached --others --exclude-standard`，并用实际存在性过滤；纳入未跟踪但未被忽略的当前源码，排除工作区中已经删除的文件。
- 逐文件审计 148 个逻辑文件：103 个 `.gd`、38 个 `.glsl`、3 个 `.py`、3 个 `.ps1`、1 个 `.js`。
- 逐文件审计 20 个运行配置、场景和文本资源：1 个 `project.godot`、1 个 `plugin.cfg`、2 个 `.json`、6 个 `.tres`、10 个 `.tscn`。
- 另审计 11 个仓库边界配置：`.editorconfig`、`.gitattributes`、`.gitignore` 和 8 个 `.gdignore`。合计覆盖 179 个逻辑或配置文件。
- 审计开始时另有 203 个非执行文件，不逐行判定代码：103 个 `.uid`、42 个 `.import`、23 个既有 `.md`、13 个 `.svg` 以及二进制 mesh、Houdini、纹理、golden 数据。发现影响运行契约的陈旧文档或场景 metadata 时仍单独列出。
- `A` 表示环境、引擎能力、旧接口、旧 schema、数据格式、路径、资源生命周期等兼容逻辑。
- `B` 表示同一入口接受多种类型、字典形状、别名键、维度、stride、通道、模式或调用签名的适配逻辑。
- 普通业务分支和单纯越界保护不算兼容层；只有承担不同合法输入或运行条件时才列入。

风险级别：`P0` 为当前数据下已确认的正确性问题；`P1` 为跨模块协议、GPU ABI、资源所有权或可移植性风险；`P2` 为局部静默 fallback、重复适配或清理项；`P3` 为低风险整理。

## 总体结论

项目不是“缺少适配”，而是适配边界过多。相同数据在 `Dictionary`、Resource 属性、动态方法、typed array、packed bytes、resident RID 和 debug readback 之间反复归一化，导致生产路径、迁移路径和调试路径难以区分。

目前已有几个应作为单一入口保留并加强的兼容中心：

- `VariantUtils`、`VoxelGeneral`：Variant、坐标、颜色和别名键归一化。
- `BufferUtils`、`PushConstantLayout`、`DebugBufferSet`：CPU/GLSL ABI 和二进制布局。
- `GodotComputeShaderBase`、`ComputeKernel`、`ComputePassChain`：RenderingDevice、RID 所有权和 dispatch。
- `TargetSceneVoxelGenerator`、`TargetReadBufferBorrow`、`VoxelPlacementTargetReader`：TargetSV 格式和 resident handoff。
- `ReportSchema`：输出字典的冻结键和诊断分级。

优化重点不是再增加一层“万能兼容器”，而是让旧输入只在上述边界转换一次，内部只流动 canonical 数据。

## 最高优先级

| 优先级 | 文件与位置 | 现状 | 优化动作 |
| --- | --- | --- | --- |
| P0 | `demos/core-SPA-scene-placement-actor/spa_interactive_demo.gd:1954-1962` | 当前 manifest 明确是 `RGBA8/R8`，该函数却用 `idx * 16` 和 `decode_float()` 读取 visual、用 `idx * 4` 读取 collision。直接 `select_data_voxel(TargetSV)` 和 GPU 空命中回退会得到错误值或大量零值。 | 禁止 demo 自行解释 TargetSV bytes；调用唯一 codec，或让 `TargetSVSetup` 返回 canonical voxel record。新增当前 `.rgba8/.r8` 的直接选择测试。 |
| P1 | `scripts/voxel_pick_gpu.gd:244-259` | TargetSV GPU 缓存只比较字节长度；内容同尺寸更新时继续使用旧 buffer。现有 smoke test没有覆盖第二次同尺寸更新。 | 以 producer revision/hash 判定更新，或使用 `buffer_update`；增加同尺寸两次写入测试。 |
| P1 | `scripts/target_scene_voxel_generator.gd:705-779`、`shaders/target_scene_voxel.glsl:87-90` | host 未验证三张输入图宽高一致，shader 对同一坐标执行 `texelFetch`；dirty rect 每次从新零缓冲开始，却返回完整结果语义。 | 入口校验全部尺寸；将 full rebuild 与持久增量更新拆成两个 API。 |
| P1 | `scripts/scene_voxel_field_builder.gd:114-128,233-340,502-511,704-786` | 多个 GPU 失败分支返回原图、空 records 或空白 collision，调用方可能把语义缺失当成功。 | 统一 `{ok, reason, output}`；只有明确的 debug/migration 模式允许保留旧值。 |
| P1 | `scripts/gpu_autoobject_runtime.gd:668-699` | dirty delta 追加失败后未回滚已经更新的 alive/state，写回不具事务性。 | 预留容量后再提交状态，或失败时回滚；增加容量耗尽测试。 |
| P1 | `addons/meshfill_editor/meshfill_editor_plugin.gd:1133-1388` | 编辑器 bridge 支持动态 `set/callv`，但缺少完整请求 schema、数组长度检查和命令白名单。 | 建立 RPC DTO、参数校验和允许方法表；错误响应保持统一字典形状。 |
| P1 | `shaders/occupancy_slice_image.glsl:10`、`shaders/resample_collision_field.glsl:10`、`shaders/collect_sv_anchors.glsl:19` | workgroup 分别为 1024、1024、512 threads；当前 RTX 4080 可运行，但 Vulkan 最低能力不保证这些尺寸。另有四个 256-thread shader。 | 收敛到保守的 64/128 或 8x8，并同步 host dispatch；在目标设备上查询/验证 limit。 |
| P1 | `project.godot:35` | `meshfill_editor/bake_fbx_python` 硬编码 `D:/Python310/python.exe`。 | 移到每用户 `EditorSettings`、环境变量或自动发现，项目配置只保存空值/相对策略。 |
| P1 | 多个 resident shader | `asset_lookup`、profile、pivot、record range 只依赖 host 保证，push 中没有对应总容量。 | 为每个可变 range 传容量并在 shader 侧拒绝越界；由同一 schema 生成 host 和 GLSL 常量。 |
| P1 | `scripts/utils/buffer_utils.gd:36-39` | `decoded_record_count()` 未拒绝 `byte_stride <= 0`，可取模或除零。 | 入口验证正 stride，并补零/负 stride 测试。 |

## 兼容策略边界

| 路径 | 无 RenderingDevice | CPU fallback | 允许的兼容内容 |
| --- | --- | --- | --- |
| 运行时 placement/prefilter/writeback | 阻断或显式不可用 | 不允许冒充成功 | local RD 到 global RD 仍是 GPU fallback；resident RID 必须同设备、有效且字节数匹配 |
| debug decode/preview/readback | 可 SKIP 或返回诊断 | 可以，但必须标记 backend/source | legacy bytes、短 readback 和截图不可用可在工具边界处理 |
| 编辑器点选 | 可回退 CPU ray | 可以，但必须显示 `pick_backend` | GPU 与 CPU 命中语义可能不同，不得静默 |
| 测试 | GPU 子项 SKIP | 不得把 CPU 结果报告成 GPU PASS | headless 只做纯契约；最终 GPU 验收必须走非 headless Vulkan |

## 收敛目标

1. 修复当前格式和缓存正确性，再进行结构重构。
2. 为 TargetSV、tile/delta、descriptor/probe、report 和 hash 增加明确 `schema_version`。
3. 引入 `GpuDeviceContext`，统一 `{device, ownership, capabilities, sync_policy}`，保留 `GodotComputeShaderBase` 为唯一 RID 生命周期实现。
4. 为 buffer 建立生成式 ABI 单一真源，统一 binding、push、stride、容量和 shader 常量。
5. 把 `VariantUtils`、`VoxelGeneral` 设为输入边界；生产内部禁止继续传播别名键和宽松 Variant。
6. 把 volume-score provider、runtime writeback、editor host 改成显式协议，减少 `has_method/callv`。
7. 给 legacy 分支增加命中统计和删除门槛；确认零调用后删除 orphan shader、死参数、死别名和不可达 fallback。

## 核心脚本逐文件索引

| 文件 | A 兼容性 | B 多输入适配 | 优化结论 |
| --- | --- | --- | --- |
| `scripts/asset_descriptor.gd` | `1-7,43-54` editor 执行、profile/direct 双来源 | `63-98,113-149` collision/pivot fallback，首参按 Mesh 或 density 分派两套签名 | P1 拆分显式 probe API；统一字段权威 |
| `scripts/asset_descriptor_baker.gd` | 无明确 | `12-102,107-147` 过滤非字典 voxel，字段默认、alpha→complexity、可选 config | P2 增加输入 schema，坏记录不能静默跳过 |
| `scripts/auto_asset_factory.gd` | `7-33,49-62` 资源路径、缓存/current/fallback mesh、导入树和 UE collision helper | 四级 mesh 解析 | P2 改用类型化加载请求 |
| `scripts/auto_object.gd` | `53-66,739-746` 旧 `voxel_descriptor` 属性和 `voxel_write_spec` metadata | `69-99,707-727` collision、pivot、spacing 默认 | P1 记录旧键命中，迁移资源后删除 |
| `scripts/auto_voxel_profile.gd` | 无明确 | `21-42` collision 归一，同时输出 `voxel`/`local_pos` | P2 边界保留旧键，内部只留 canonical 坐标 |
| `scripts/auto_voxel_runtime_profile_container.gd` | `83-143` local/global/external RD 与 owned/borrowed 生命周期 | `345-500,613-795,857-1050` descriptor/profile、方法/属性、两套 probe 签名、Array/Dictionary/颜色/坐标 | P1 建立版本化 DescriptorAdapter |
| `scripts/autoobject_probe_prefilter_gpu.gd` | `341-343,933-946` indirect dispatch 能力、动态 provider/RID | `949-990` profile ID 从 metadata/property/write spec/descriptor 多来源解析 | P1 profile ID 只保留一个来源 |
| `scripts/core_demo_contract_fixture.gd` | `40-121,157-208` editor/runtime、无 RD SKIP、禁止 CPU 冒充 GPU PASS | 结果字典默认字段 | 应保留；抽取统一 GPU 测试结果构造器 |
| `scripts/fly_camera.gd` | `25-89,114-126,190-235` editor-only、离树、root/current/owner/parent 多宿主 | Label3D 或动态 `get_aabb` selectable | P2 复用统一 scene-host locator |
| `scripts/godot_compute_shader_base.gd` | `48-149,171-325,385-441,741-815` local/global/external RD、owned/borrowed RID、延迟 GC、源码/导入 shader 双加载 | helper options、scope 和资源类型 | P1 指定为唯一设备与生命周期入口 |
| `scripts/gpu_autoobject_runtime.gd` | `555-611,953-1158` 阻止跨 RD；CPU-packed legacy 与 resident writeback 并存 | `785-805,2134-2237` 两套 `update_profile`，profile/flags/bounds 多类型别名 | P1 拆签名；修复非事务 dirty delta |
| `scripts/mesh_voxelizer_gpu.gd` | `61-91` 无 RD 明确失败 | `198-219` indexed/unindexed surface | P2 提取共享 mesh surface adapter |
| `scripts/non_headless_scene_guard.gd` | `8-45` 区分 headless 场景和 `--script/-s` 测试 | 无明确 | 与插件 headless 检查合并成单一启动策略 |
| `scripts/scene_placement_actor.gd` | `125-183,1433-1584,2486-2543` 共享 RD、same-device runtime、non-resident debug/legacy readback | `842-899,1024-1061` brush 字典和可选 target collision | P1 分离生产 resident API 与 debug readback |
| `scripts/scene_voxel.gd` | 来源记录兼容投影 | `22-62,76-117` 公共/内部字段、非字典过滤、`base_pixel`/ID fallback、可选 collision | P2 类型化 SceneVoxel + schema version |
| `scripts/scene_voxel_committer.gd` | `81-105` 失效 `_enable_gpu` 参数；`331-390` GPU collision 失败回 CPU field；`921-989` tile-store/field-builder facade delegates | dirty flags 可为多形态，委托保持旧调用面 | P1 决定其是正式 facade 还是迁移层；不要保持当前含糊状态 |
| `scripts/scene_voxel_debug.gd` | GPU 未就绪返回空 typed buffers，resident count 回退旧字段 | `22-28,51-81` 多套 object/source ID 别名 | P2 复用统一 ID adapter |
| `scripts/scene_voxel_field_builder.gd` | 多个 GPU 失败分支回原图/空数据 | compare mode、图像格式和记录输入 | P1 改显式失败，禁止静默丢 stamp/collision |
| `scripts/scene_voxel_source_record.gd` | 无独立环境兼容 | `5-29` Brush/Target 默认字段、tick clamp、缺失 modified/target pipeline | P2 source type 改 enum + versioned record |
| `scripts/scene_voxel_target.gd` | 无明确 | `14-30` target mix/pipeline 默认和 dirty flags | P3 `tick` 未使用，迁移后删除 |
| `scripts/scene_voxel_tile_codec.gd` | legacy float、RGBA8/R8 迁移布局 | `144-176,203-227,261-394` flags 接受字典/数组/字符串/掩码，delta 多边界和 removed 别名，标量/vec4 | P1 确定 canonical tile/delta/field schema；无调用的 legacy float helper 待删 |
| `scripts/scene_voxel_tile_store.gd` | `453-530,558-566,1298-1457` 旧 2D/new 3D tile 双写、legacy hash/numeric refs | `715-745` delta/profile/bounds/flags 别名 | P1 退役双维护；`1068-1078` 字符串化且已 GC 的“resident RID”应删或真正借用 |
| `scripts/scene_voxel_volume_channels.gd` | 旧字段名 | `8-25` `y_min/min_height`、`y_max/max_height` | P2 资源迁移后只保留 canonical 键 |
| `scripts/semantic_probe_generator.gd` | 空 Mesh fallback、probe 新旧字段 | `47-101,139-163,196-233,589-640` indexed/unindexed、collision 坐标别名、多候选来源 | P1 版本化 probe schema；共享 surface adapter |
| `scripts/shared_property_type.gd` | descriptor 方法/属性双接口 | `25-71` collision 可为 Array 或数值，color/complexity fallback | P1 保留为唯一边界，内部输出 canonical 类型 |
| `scripts/spa_editor_contract.gd` | provider 无 AABB API 时退回 world positions | `134-169,247-291` `display_key/key`、两类 provider pick 接口 | P2 定义显式 provider 协议 |
| `scripts/target_scene_voxel_generator.gd` | `132-212,254-680` RGBA8/R8 与 legacy RGBA32F/R32F，GPU/CPU debug decode | 部分通道、零填、completeness 多来源、stats-only | P1 作为唯一 TargetSV codec；加 schema 和尺寸校验 |
| `scripts/target_sv_loader.gd` | `67-84,200-230` `res://` Resource 与 `user://`/绝对路径；GPU decode 失败转 CPU | metadata 默认值、格式提示 | P1 debug CPU fallback 要与生产 GPU-only 隔离 |
| `scripts/terrain_config.gd` | 无明确 | `36-45` 任意 overrides 覆盖默认 | P3 加允许键和类型校验 |
| `scripts/terrain_initializer.gd` | `34-71,74-128` host/current/owner/parent/group/name 多路径；异常尺度修正 | 缺 metadata 时推断分辨率并重采样 | P2 建立 TerrainHost 接口和来源报告 |
| `scripts/volume_score_provider_registry.gd` | 动态 provider 生命周期 | `28-47` 方法存在性与弱约束 provider | P2 类型化 provider 与统一注销策略 |
| `scripts/voxel_pick_gpu.gd` | SceneVoxel/TargetSV 两模式，缺 terrain 用 1x1 零场 | `41-163,273-308` 两组尺寸、ray、threshold 输入 | P1 修复同尺寸陈旧缓存 |
| `scripts/voxel_placement_generator.gd` | `911-942` resident 借用、float/RGBA8 上传、零 collision 多路径 | quota/result capacity 等别名与多级 settings | P1 resident-only 后上传分支疑似不可达，移到 debug 或删除 |
| `scripts/voxel_placement_output.gd` | `39-86` legacy 单 pivot push 与 per-record pivot RID；无 RD 阻断 | 两种 pivot 输入 | P2 生产只保留 per-record pivot |
| `scripts/voxel_placement_state_chain.gd` | `10-133` full readback、compact CPU delta、resident GPU 三链 | 多种 state payload | P1 生产只留 resident，其他移入 debug/migration |
| `scripts/voxel_placement_target_reader.gd` | `84-101` resident-only、same-device、RID/byte count 校验 | `71-81,94-133` collision 截断/零填、顶层/nested handoff 形状 | P1 producer 只发 canonical handoff；删冲突上传 fallback |
| `scripts/voxel_placement_writeback.gd` | `29-44,349-360` 动态 GPU spawn API、同 RD 挂载 | `306-469` common/asset/per-asset 配置优先级和动态结果形状 | P2 定义 RuntimeWriteback 接口 |
| `addons/meshfill_editor/meshfill_brush.gd` | `52-64` editor-only | `102-137,177-219,249-301` TargetSV 默认、terrain ray/地面 fallback、显示通道、per-voxel 数组长度 | P2 类型化 display 协议，先验证分辨率 |
| `addons/meshfill_editor/meshfill_editor_plugin.gd` | `210-1045` 动态 host；`1510-1684` JSON v2/旧文本锁、Windows `tasklist`/其他平台 `ps` | `1133-1388` bridge 分包/多行 JSON、路径别名、Array→Vector3、动态 set/call | P1 host adapter、RPC schema/白名单、项目键原子锁；headless guard 前移 |

## 通用工具脚本逐文件索引

| 文件 | A 兼容性 | B 多输入适配 | 优化结论 |
| --- | --- | --- | --- |
| `scripts/utils/brush_voxel_display_gpu.gd` | RD/RID 由基类承担 | `27-55,102-118` 空 terrain dummy、逐实例 RGBA 或统一 brush color、参数默认 | P2 非空错误长度应失败，不应退统一色 |
| `scripts/utils/buffer_utils.gd` | vec3→vec4、Transform→mat4 对齐；shader/semantic 两种 RGBA8 字节序 | `10-126,180-183` 截断/零填、offset fallback、ints+floats | P1 引入 strict/legacy decode policy；修 zero stride |
| `scripts/utils/canonical_hash.gd` | key 排序、类型前缀、float 量化和 ±0 归零维持稳定 hash | `21-76` 多 Variant；Array 可按无序集合排序 | P1 hash 加版本；禁止未知类型走 `str()` |
| `scripts/utils/compute_kernel.gd` | WeakRef owner、preload、owned/borrowed RD submit | `66-128` direct/indirect、有/无 push、多 set、1D/2D/3D groups | P1 使用 ComputePassDesc 和 DeviceOwnership |
| `scripts/utils/compute_pass_chain.gd` | global RD 需额外 sync policy | pass 字典支持 barrier/sync、direct/indirect、可空 push、多 set | P1 必填字段不能默认 `groups=ONE` |
| `scripts/utils/debug_buffer_set.gd` | raw GLSL 无 include 的 codegen、magic/version/reserved 和 fixed-point decode | schema、slot/channel、set/binding override、外部 bytes/GPU readback、短数据 | P1 构造时完整验证；短 readback 返回 completeness |
| `scripts/utils/demo_assets.gd` | FBX/GLB/glTF/OBJ/DAE/Blend/Mesh/Resource/Scene；直接 Mesh/PackedScene/MeshInstance/ImporterMeshInstance | 自定义扩展、collision helper fallback、tangent/normal 降级、`mesh_transform/transform` 双键 | P1 建 MeshLoadResult 和 importer adapter |
| `scripts/utils/demo_contract_utils.gd` | `res://`/相对路径、TSCN 引号/无引号、旧 `source_doc`/新 `source_docs` | 扩展过滤、大小写策略、Variant→String | P2 metadata 统一数组键；Markdown 不再靠括号扫描 |
| `scripts/utils/demo_debug_visuals.gd` | 无 | 无明确 | 无兼容性清理项 |
| `scripts/utils/demo_ui.gd` | editor/runtime 的 viewport、固定路径、名称、parent、owner 多级 camera fallback | 布尔参数组合搜索顺序 | P2 改依赖注入/group；无调用的 `find_any_camera` 待删 |
| `scripts/utils/display_channel_utils.gd` | 保留旧 inline enum 数值和材质表现 | channel→view mode，未知值回 Color | P2 enum 冻结；未知值应显式 invalid |
| `scripts/utils/fs_utils.gd` | `res://`、`user://`、绝对路径 | raw RGBAF 按 width/height 校验，可选错误前缀 | P2 统一 path resolver，校验正尺寸和乘法范围 |
| `scripts/utils/hash_utils.gd` | 0→1 sentinel 保持旧 ID 约定 | hex 大小写，当前忽略任意非 hex 字符；String/bytes FNV | P2 增 strict hex parser |
| `scripts/utils/input_sim.gd` | 同时写 keycode/physical_keycode 和 position/global_position | modifier、pressed、button | P2 统一消费者后删除双写 |
| `scripts/utils/placement_result_codec.gd` | per-record pivot RID 与 legacy shared pivot；无 RID 时 dummy binding | duck-typed owner、record/rotation 计数归一 | P1 拆两个显式入口，统计 legacy 命中 |
| `scripts/utils/placement_shared_glsl.gd` | codegen 替代不支持 include 的 source compiler | yaw 连续/snap 两种 offset；block 名称 | P1 未知 block 应失败而非空串 |
| `scripts/utils/push_constant_layout.gd` | std430、vec3 对齐、16-byte 总长、padding 补零 | schema `[name,type]`；多 vector/color/quaternion/plane/Array/PackedArray | P1 schema 错误使 layout 无效，缺字段不得继续发零 |
| `scripts/utils/report_schema.gd` | 冻结旧报告键、success/blocked/empty 变体和 bridge/test consumer | core/contract/diag/derived、failure defaults、runtime extra | P1 加 schema version 和类型化 builder；删键需同批迁移 consumer |
| `scripts/utils/scene_tree_test.gd` | 无 | `stop_on_failure` 支持短路/继续 | P3 可拆成两个命名入口 |
| `scripts/utils/score_timing_profiler.gd` | 无 | 无明确 | 无兼容性清理项 |
| `scripts/utils/shot_utils.gd` | 无 viewport/texture/image 时可 `ok+skipped` | image diff 接受 null/non-Image、不同尺寸取交集、step/hash/frame 参数 | P1 SKIP 与 PASS 分离；默认要求同尺寸 |
| `scripts/utils/sv_field_scatter.gd` | 无明确 | 固定 8-float record，overwrite/max 两种 write mode，slot 字典 | P2 mode 用 enum；验证 slot 长度 |
| `scripts/utils/target_read_buffer_borrow.gd` | 顶层/nested summary、多 RID 键、旧 byte count/format/owner 默认；严格同 RD/RID/size | caller 提供候选 keys，collision 可选 | P1 建 versioned TargetReadBufferHandoff；三重校验必须保留 |
| `scripts/utils/target_sv_lookup.gd` | edited root、父链、parent subtree、owner subtree 多拓扑 | 5 个布尔参数控制策略 | P2 用 SearchStrategy/group，避免选错多个 setup |
| `scripts/utils/target_sv_setup.gd` | 旧/不完整 metadata 默认、无主 RD SKIP、`switch_channel` 旧 alias | options、短 field fallback、fields/params/options 三层字典 | P2 versioned metadata；确认外部调用后删死 alias/getter |
| `scripts/utils/test_utils.gd` | main/local RD；英文和历史 mojibake 关键词 | 文件收集的排除/扩展/错误前缀 | P1 不按 ANSI 重读；确认样本后删除 mojibake 兼容字面量 |
| `scripts/utils/variant_utils.gd` | 可选对象属性、多 key alias | Color/Vector/RID/int ID/bool 接受多类型与文本形式 | P1 作为全项目边界 normalizer；增加失败来源诊断 |
| `scripts/utils/volume_score_3d.gd` | 缺 owner/Terrain/有效 mesh 时平坦零高度 | scene fields 默认、spacing clamp、短 terrain array fallback | P2 返回 `{ok,source,field}`，区分真实地形和 flat fallback |
| `scripts/utils/voxel_debug_label.gd` | 无 | 默认参数覆盖旧 demo 样式，billboard/width 切换布局 | P3 样式继续增长时改 preset |
| `scripts/utils/voxel_display.gd` | writer 生命周期绑 tree exit；ArrayMesh surface material/普通 Mesh material 属性 | centers/Transforms 两种实例输入、短 colors 补白、三种 duck-typed writer | P1 生命周期保留；非空错误颜色长度应失败 |
| `scripts/utils/voxel_field_display_gpu.gd` | 空 terrain dummy SSBO | occupancy/color 必需，collision 错长零填，params 默认 | P2 required/optional schema；非空错误 collision 应失败 |
| `scripts/utils/voxel_fixtures.gd` | fixture 仍构造旧 offset/radius/height collision primitive | 默认 tile/origin、可变 TargetSV texture/slice 和强碰撞 index | P2 fixture 改 canonical voxel/local_pos，格式常量从 codec 导出 |
| `scripts/utils/voxel_general.gd` | legacy scalar→vec4、collision 多坐标 alias，过滤停用 shape | resolution/grid/tile/index；Vector3i/3/2/Array/Dictionary x-y-z 或 r-g-b | P1 分 strict geometry 与 legacy normalizer；无调用 adapter 待删 |
| `scripts/utils/voxel_instance_display_gpu.gd` | 基类承担 RD | 12-float transform；精确 colors 或白色 dummy + `use_colors` | P2 空 colors 才是默认，非空错误长度应失败 |
| `scripts/utils/voxel_multimesh_writer_gpu.gd` | 无主 RD、动态检测 Godot build API、render-thread free、外部 MultiMesh 失效 | 空 bytes→4-byte dummy，RID/count 校验 | P1 集中 capability 探测；生命周期 guard 不可简化 |

## Shader 逐文件索引

38 个 shader 均使用 `#[compute]` 和 `#version 450`，只支持 Godot `RenderingDevice`/Vulkan；没有 Compatibility renderer 或 CPU fallback。表中的 host 位置是必须同步修改的 ABI 另一端。

| 文件与 host | A 兼容性 | B 多输入适配 | 优化结论 |
| --- | --- | --- | --- |
| `shaders/autoobject_apply_accepted_placements.glsl`；`GPUAutoObjectRuntime._try_apply_accepted_placement_record_shader()` | 固定 128B CPU-packed legacy record，与 resident shader 并存 | record/runtime/dirty/stats 容量、epoch | P2 两套 writeback 共享块生成化；验证 object ID 唯一 |
| `shaders/autoobject_apply_accepted_placements_resident.glsl`；`_dispatch_accepted_placement_resident_shader()` | 单资产 legacy push、mixed asset lookup、空 stamp bounds→origin | `meta.w` 模式、grid/stats/dirty 容量 | P1 lookup 缺 asset capacity |
| `shaders/brush_voxel_instances.glsl`；`BrushVoxelDisplayGPU.write_brush()` | terrain 缺失/越界回零，每 voxel color 取代旧 batch color | count、XZ、slice、terrain length、scale | P3 `height_span`/`brush_rgba` push 已不消费 |
| `shaders/collect_sv_anchors.glsl`；`AutoObjectProbePrefilterGPU._dispatch_collect()` | linear dirty dispatch 可拆 X/Y，保留退休 push 槽 | grid/tile、dirty count、capacity、target threshold；alpha/collision 取 max | P1 512 threads；counter 超 capacity 契约需明确 |
| `shaders/compact_scene_voxel_tile_summaries.glsl`；tile store reduce | 动态 summary/compact stride | tile count、两个 stride | P2 最小字段数 schema 化；atomic 输出顺序不稳定 |
| `shaders/compose_blend_sv_fields.glsl`；`ScenePlacementActor.compose_blend_sv_fields()` | committed SV + BrushSV overlay | voxel count 与 packed-R8 word count | P2 host 验证四个 RID 容量/grid |
| `shaders/fine_score_dispatch_finalize.glsl`；VPG finalize | resident count→indirect args，空 count→零 dispatch | anchor grid X、topk、capacity | P2 grid X 与 scorer 常量单一真源 |
| `shaders/init_scene_voxel_tile_summaries.glsl`；tile store | dynamic stride、min sentinel | tile count/stride/sentinel | 可并入 tile summary schema |
| `shaders/init_stamp_bounds.glsl`；VPG | stride>=2 的扩展布局 | bound count/stride | 无明确风险 |
| `shaders/merge_sv_collision_records.glsl`；field builder | packed R8 CAS max，多 record 争用 | grid/slice/voxel/record count | host 已归一坐标，无明确风险 |
| `shaders/occupancy_slice_image.glsl`；committer image pass | RGBA16F sampler→R32F image，源/目标/base 尺寸 | channel 0-3、epsilon、三套尺寸 | P1 1024 threads；host 断言真实 source size |
| `shaders/pack_prefilter_field_pair.glsl`；prefilter | resident RGBA8/R8→float2，另有 CPU float pack | voxel count | P2 只保留一个 authoritative pack |
| `shaders/pack_target_field.glsl`；VPG/SPA | canonical RGBA8→vec4 target field | voxel count | P2 shader 4B push/host 16B push 统一声明或做反射测试 |
| `shaders/pick_scene_voxel.glsl`；`VoxelPickGPU` | mode 0 SceneVoxel raymarch / mode 1 TargetSV；未知 mode 回 scene | ray/grid/voxel/terrain/target/radius/threshold | P1 修缓存；验证 terrain size |
| `shaders/placement_result_score_sum.glsl`；VPG | resident result 小归约 | capacity/record stride | P1 256 threads portability；stride 必须>=4 |
| `shaders/placement_results_to_world.glsl`；`PlacementResultCodec` | shared push pivot/per-record pivot | record/rotation/in/out stride、origin、voxel size | P1 pivot capacity；stride 必须>=4 |
| `shaders/prefilter_anchor_dispatch_finalize.glsl`；prefilter | score/top-K 两套 indirect args，空 count 零 dispatch | grid X、asset blocks、capacity | 无明确风险 |
| `shaders/prepare_target_read_buffers.glsl`；无 host | color/occupancy valid flag、缺失零填、legacy 命名 | voxel count、两个 valid flag | P2 orphan shader，确认外部无动态 load 后删除 |
| `shaders/reduce_anchor_candidates.glsl`；VPG | mixed-asset common pool，quota<=0 无限 | topk、各容量、min distance、per-asset quota | P1 单 invocation 串行扫描，改分块并行归约 |
| `shaders/reduce_scene_voxel_tile_summaries.glsl`；tile store | RGBA8 + packed R8 | grid/tile/stride/threshold/quant scale | P2 拒绝 stride<6 |
| `shaders/resample_collision_field.glsl`；field builder | R32F 任意 src/dst 尺寸 | out/src/base resolution | P1 1024 threads；base/source 一致性 |
| `shaders/sample_r32_pixel.glsl`；field builder | clamp 任意像素 | width/height/pixel | 无明确风险 |
| `shaders/scatter_sv_field_records.glsl`；`SvFieldScatter` | overwrite BrushSV / max-alpha committed SV | dims/count/write mode/color/complexity/collision | P2 未知 mode 不应默认 overwrite |
| `shaders/scene_voxel_tile_object_ref_update.glsl`；tile store | serial/parallel、两种 dirty bit schema、可选 stats | grid/tile/ref/capacity/flag/worklist/schema/mode | P2 host 只用 serial；删除虚假 resident RID 输出 |
| `shaders/score_anchor_asset_probes.glsl`；prefilter | 权重替代 probe kind、trilinear sampling、resident count | grid、probe ranges、signed weights | P1 256 threads；range 加总容量 |
| `shaders/score_anchor_asset_residual.glsl`；VPG | mixed descriptor、pivot/yaw/Target/debug 多模式 | 六组 push、四个 resident descriptor buffer | P1 range capacity；`ANCHOR_GRID_X` 单一真源；shared 约 5888B 可接受 |
| `shaders/score_blendsv_feedback.glsl`；SPA | target collision 可选，缺失只读 visual alpha | count/has_collision/epsilon/quant | dummy buffer 分支正确，无明确风险 |
| `shaders/select_anchor_topk.glsl`；prefilter | resident count、threshold filter | asset count/grid X/min score | P1 256 threads；TOPK 常量集中 |
| `shaders/stamp_asset_voxels.glsl`；VPG | mixed asset、clearance skip、single/dual write、write window | grid/rotation/profile/delta/scales/dual/voxel size | P1 profile/pivot/range 加容量；CAS 共享块生成化 |
| `shaders/stamp_collect_voxel_disc_3d.glsl`；field builder | separate input/output 防边缘读写互扰 | compare mode 0/1/2、dims/disc/value/quant | 无明确风险 |
| `shaders/stamp_rgba_channel_disc.glsl`；field builder | RGBA16F source/output | width/height/center/radius/channel/value | 无明确风险 |
| `shaders/target_scene_voxel.glsl`；target generator | optional mask 零图、dirty rect、packed outputs+preview | height/capture/span/slope/texture/slice/rect | P1 256 threads、三图尺寸、持久增量语义 |
| `shaders/target_sv_pack_read_buffers.glsl`；target generator | completeness input/collision-derived/visual-only；stats-only/full output | 三个 flags、voxel count | optional dummy buffer 分支正确 |
| `shaders/terrain_collision_volume.glsl`；field builder | 2D R32F→多 slice packed R8 | XZ/source/count/epsilon | P3 `total_slices` 未消费 |
| `shaders/voxel_collision_erode.glsl`；mesh voxelizer | `min_neighbors=0` legacy 默认6 | grid、neighbor threshold、strength | P3 `core` 局部变量未使用 |
| `shaders/voxel_field_instances.glsl`；field display | 四种 view mode，未知 mode 回 project color，空 voxel 零 basis | field/dims/slice/scale/threshold/terrain length | P2 host clamp 正 `xz_res/slice_count`，避免除零 |
| `shaders/voxel_instance_copy.glsl`；instance display | color 可选，缺失白色 | count/use_colors | 无明确风险 |
| `shaders/voxelize_mesh_solid.glsl`；mesh voxelizer | X/Y/Z parity 多数票适配近似 watertight mesh；semantic RGBA8 顺序 | grid/tri count/AABB/cell/color | P1 性能 `voxels*triangles*3`；保留明确字节序转换 |

## 配置、场景与文本资源逐文件索引

| 文件 | A 兼容性 | B 多输入适配 | 优化结论 |
| --- | --- | --- | --- |
| `.codex/hooks.json` | `command`/`commandWindows` 双字段 | 空 matcher 匹配全部 prompt | P2 依赖 PATH/cwd 的 `python`，改解释器解析 |
| `addons/meshfill_editor/plugin.cfg` | Godot plugin format | 无明确 | P3 补最低 Godot 4.6/Forward+ 启动校验 |
| `assets/target_sv/target_sv_point_cloud.json` | canonical `rgba8_unorm/r8_unorm`，consumer 兼容旧 float | point cloud+height、relative/guidance flags、attribute map、维度/路径 | P1 加 manifest `schema_version`；Houdini `fileversion` 不是 schema |
| `assets/terrain/utils_terrain_material.tres` | Godot resource format3 | 无明确 | 无兼容清理项 |
| `assets/vegetation/sm_test_leaf_test2_asset.tres` | embedded mesh/source mesh/source path 三来源 | color/complexity/collision/subtype/layer/group | P2 约905KB，来源权威不清，外部化 payload |
| `demos/asset-descriptor-demo/asset-descriptor-demo.tscn` | embedded imported ArrayMesh + source metadata | rock/tree 四资产、材质/mesh/bounds/UI | P2 约2.17MB，改实例化外部资源 |
| `demos/asset-descriptor-demo/baked_descriptors/Geo_cliff_01_descriptor.tres` | mesh/probe/profile/top-level 字段多层 | metric weights、collision、profile、source path | P2 外部化并明确 descriptor/profile 权威 |
| `demos/asset-descriptor-demo/baked_descriptors/Geo_cliff_02_descriptor.tres` | 同上 | 同上 | P2 约1.31MB/23523行，重复最重 |
| `demos/asset-descriptor-demo/baked_descriptors/Geo_SM_Ground_Grass_descriptor.tres` | mesh/profile/probe | color/complexity/probes/type | P2 外部化 baked payload |
| `demos/asset-descriptor-demo/baked_descriptors/Geo_SM_TestLeaf_Test2_descriptor.tres` | mesh/profile/probe | vegetation、collision、probes | P2 top-level/profile alpha 不同，明确权威 |
| `demos/core-meshfill-framework/core-meshfill-framework.tscn` | 复用公共 setup | 无明确 | 无兼容清理项 |
| `demos/core-scene-voxel-field-system/core-scene-voxel-field-system.tscn` | 复用公共 setup | 无明确 | 无兼容清理项 |
| `demos/core-scenevoxeltile/core-scenevoxeltile.tscn` | debug script + 公共 setup | 无明确 | 无兼容清理项 |
| `demos/core-SPA-scene-placement-actor/core-scene-placement-actor.tscn` | non-headless Vulkan；无 RD 只 SKIP | `selection_mode=1` | 契约清晰，应保留 |
| `demos/core-sv-anchor-collection/core-sv-anchor-collection.tscn` | GPU required，共享 SPA demo | `selection_mode=3`、禁止 auto spawn | P2 metadata 仍描述已删除的 scene/collision/support gate |
| `demos/placement-autoobject-probe-prefilter/placement-autoobject-probe-prefilter.tscn` | 复用公共 setup | 无明确 | 无兼容清理项 |
| `demos/placement-score-3d/placement-score-3d.tscn` | 复用 SPA/setup | 一次输入 leaf/cliff1/cliff2，`selection_mode=3` | 多资产 fixture 合理 |
| `demos/target-sv-point-cloud-conversion-c/target-sv-point-cloud-conversion.tscn` | editor brush script | `guidance_visible=true` | 无兼容清理项 |
| `demos/utils_demo_setup.tscn` | NonHeadless guard、Forward+、共享 camera/light/TargetSV/terrain | 可替换 ext resources | P2 SSAO 进一步绑定 Forward+，GPU demo 合理 |
| `project.godot` | skeleton import compatibility、Godot 4.6 Forward+、禁 FBX importer | tile size、plugin、bake Python | P1 删除机器绝对路径；P2 明确 demo/plugin repo 无 main scene |

## Demo、测试与工程工具逐文件索引

| 文件 | A 兼容性 | B 多输入适配 | 优化结论 |
| --- | --- | --- | --- |
| `.claude/cleanup_agent_worktrees.ps1` | lock、活跃时间、未合并提交保护 | `AgeHours` | P1 分支名硬编码且吞错后强制删除；用 porcelain 映射并检查 dirty/exit code |
| `.claude/clean-worktrees.ps1` | PowerShell 5.1 stderr、`PSScriptRoot` fallback、Windows path/case normalize | `DryRun`，registered dir/branch 双保护 | P1 与上一脚本合并成唯一安全入口 |
| `.claude/concept-gate.ps1` | UTF-8、cwd-independent、PS5.1 JSON、fail-open | Edit/MultiEdit/Write/未知 tool payload | P2 抽统一 hook payload parser 并加样例测试 |
| `.codex/hooks/semantic_gate_user_prompt.py` | raw text fallback、UTF-8 subprocess、timeout/fail-open | prompt 多键、nested params、String/List/Dictionary、中英文 query | P2 为真实 Codex payload 建契约测试 |
| `demos/asset-descriptor-demo/asset_descriptor_demo.gd` | 小键盘归一、mtime global path fallback、EditorInterface 能力探测、外部 Python | 增量/全量 scan，rotation/scale bake 组合，probe offset Variant | P1 提取 editor asset service；覆写 FBX 前强制 backup/interpreter/SDK 验证 |
| `demos/core-scenevoxeltile/scene_voxel_tile_debug.gd` | editor viewport forwarding 取代旧 `_input`；ray hit Variant | key/mouse 两类事件、可变 dirty flags | P3 抽共享 editor input/pick adapter |
| `demos/core-SPA-scene-placement-actor/spa_interactive_demo.gd` | 动态 provider、AABB/Dictionary/OBB、GPU→terrain→plane pick、editor lifecycle | 多选择模式、Callable 的 2/3 参数、Array/PackedInt IDs、统一 selection record | P0 修 RGBA8/R8；P1 provider 协议和 pick backend 收敛 |
| `demos/core-SPA-scene-placement-actor/spa_test.gd` | same-RD attach、borrowed ownership、无 RD 路径 | multi-asset 数组/RID/settings、结果默认字段 | P1 统一 GPU fixture 和失败中途资源释放 |
| `demos/core-sv-anchor-collection/sv_anchor_collection_demo.gd` | metadata 默认、GPU decode→CPU debug fallback、短数组、optional SPA API | typed empty dirty list、layer modes、prefilter 两层 config | P2 统一 TargetSV snapshot；明确 debug CPU fallback 边界 |
| `demos/placement-autoobject-probe-prefilter/probe_prefilter_demo.gd` | 展示时跳过非字典 probe、权重默认 | probe list、density、左右选择 | P3 normalize 后再供 HUD 消费 |
| `demos/placement-score-3d/volume_score_demo.gd` | descriptor scan、缺 collision 用 AABB span、TargetSV grid rebuild、same-RD borrow、optional SPA API | multi-asset、anchor handoff、resident target settings | P1 抽 descriptor/profile/TargetSV handoff service |
| `tools/bake_fbx_transform.py` | Windows CPython 3.10 FBX SDK、ASCII writer→native writer | 9-float basis、backup/ascii/label/dry-run | P1 mirrored determinant 应拒绝或翻 winding；写入前强制原子备份 |
| `tools/diag_tree_collision_voxels.gd` | 无 | 参数化 label/path/color/min-neighbors | P2 失败累计并非零退出，不能只打印 |
| `tools/golden_snapshot_check.js` | JSON/raw response、node path→script tree fallback、CRLF normalize | method/params/timeout、runner 定位 | P1 baseline 缺失必须显式 `--approve`，不能自动创建并成功 |
| `tools/shot_asset_descriptor_demo.gd` | camera path→recursive、optional contract/debug method、截图 unavailable SKIP | 三个截图状态 | P1 默认截图不可用应失败，只有显式 smoke 可 SKIP |
| `tools/shot_brush_overlay.gd` | 无 | 无明确 | P3 接统一 screenshot harness，保留严格失败 |
| `tools/shot_spa_demo.gd` | camera missing 容忍 | 三组 camera 参数 | P1 实际只加载公共 setup，截图结果被忽略且固定成功；重写为真实 SPA 断言 |
| `tools/shot_sv_anchor_collection.gd` | 旧 demo 输入/节点形状 | Shift+G、数字4、camera | P1 调不存在的 `_unhandled_input` 并期待已删除箭头节点；改 provider forwarding + MultiMesh 断言 |
| `tools/spa_click_test.py` | GDScript bool/int/float/raw return parse | 6 selection modes、5 display keys、坐标调用 | P1 `or True` 使断言恒过、`off_ok` 未使用；改结构化严格断言 |
| `tools/test_asset_descriptor_baker.gd` | 无 | voxel list + descriptor config | P3 增缺字段、错类型、空 mesh、坐标别名负测 |
| `tools/test_auto_voxel_runtime_profile_container.gd` | 无 RD SKIP、有符号 u32 修正、borrowed RD ownership、拒绝旧 residency key | staging/upload/readback/byte-count 状态 | P1 抽共享 RD/readback fixture |
| `tools/test_autoobject_probe_prefilter.gd` | 无 RD SKIP、拒绝旧 route、same-RD borrow/other-RD block | bounds Vector3i/Array/Dictionary、两种 RGBA8 顺序、ID clean | P1 把 bounds/颜色规范固化为生产 codec |
| `tools/test_buffer_utils.gd` | 负 offset/短 buffer→Zero/Identity | 多 buffer layout 和 offset | P2 增 strict `try_decode`；补 zero-stride 测试 |
| `tools/test_compute_shader_compile.gd` | local RD→main RD、ownership、global path→`res://`、marker 清理 | 收集/编译 shader | P1 与 `verify_shader_compile` 合并成唯一递归 compile+pipeline gate |
| `tools/test_core_demo_contracts.gd` | 无 RD SKIP、headless/Vulkan 双口径、中英文旧文案 | Markdown table/link parser | P2 改结构化 manifest；当前缺 `staging` 契约而失败 |
| `tools/test_markdown_contracts.gd` | GPU/CPU command 分类、GPU SKIP、legacy alias 限定 | 多文档/多契约字典 | P2 与 core contract checker 收敛职责 |
| `tools/test_semantic_probe_generation.gd` | Variant 坐标归一、跳过非字典签名项 | density、mesh/convex/collision/interior | P3 统一 probe schema，避免局部直接 cast |
| `tools/test_stamp_collect_voxel_disc.gd` | 旧 2D→3D pass migration、无 GPU 失败 | compare mode 0/1/2 | P2 enum 替代数字，抽 CPU reference |
| `tools/test_target_sv_buffer_decode.gd` | Q8 tolerance、CPU/GPU decode、无 RD SKIP、same/other RD | optional collision、prepacked/default、stats-only | P1 prepacked preference 仍是恒过 stub，补 SPA direct selection 回归 |
| `tools/test_target_sv_point_cloud_conversion.gd` | 旧 visualization/公共 setup、optional preview、format aliases | 三种 view hotkey、snapshot | P1 复用格式断言到 SPA selection test |
| `tools/test_targetsv_brush_overlay.gd` | 无主 RD SKIP、writer keepalive/delayed cleanup、dynamic scene API | rebuild/direct GPU brush | P1 抽 GPU writer lifecycle fixture |
| `tools/test_volume_score_provider_registry.gd` | dynamic provider lifecycle | register/refresh/unregister | P3 增失效/退出/异常/重复 provider 测试 |
| `tools/test_voxel_pick_gpu.gd` | 真实 RD required、当前 RGBA8/R8 fixture | SceneVoxel/TargetSV 两入口 | P1 增同尺寸内容刷新和 GPU/SPA CPU record 一致性测试 |
| `tools/test_voxel_writer_dispose.gd` | 无 RD SKIP、PREDELETE/tree-exiting 两生命周期 | orphan/in-tree writer | P1 不应只提示检查 stderr 后固定成功；断言 error/leak count |
| `tools/verify_glsl_gen_blocks.gd` | CRLF/LF normalize | shared block、Debug schema、section/set/binding override | P1 用结构化 marker parser，检查重复/嵌套/未闭合 |
| `tools/verify_shader_compile.gd` | 无 local RD 失败、compute marker 清理 | 顶层 `.glsl` 批量编译 | P2 与另一门禁合并，递归并创建 pipeline |

## 仓库边界配置逐文件索引

| 文件 | 兼容与适配内容 | 优化结论 |
| --- | --- | --- |
| `.editorconfig` | UTF-8/LF/final newline；图片与 HIP 禁止 trim/final newline | P2 与实际 binary 扩展表同步 |
| `.gitattributes` | text auto + working-tree UTF-8；图片/HIP 显式 binary | P1 补 `.fbx`、`.raw`、`.r8`、`.rgba8`、`.res`、`.bgeo.sc` 等当前 binary 类型 |
| `.gitignore` | 隔离 Godot cache、日志、截图、IDE、本机 bridge、assistant/worktree 文件 | P3 保持；项目级绝对 Python 路径不应留在 `project.godot` |
| `demos/asset-descriptor-demo/diagrams/.gdignore` | 阻止 Godot 导入文档图 | 无输入适配，保留 |
| `demos/core-SPA-scene-placement-actor/diagrams/.gdignore` | 阻止 Godot 导入文档图 | 无输入适配，保留 |
| `demos/core-meshfill-framework/diagrams/.gdignore` | 阻止 Godot 导入文档图 | 无输入适配，保留 |
| `demos/core-scene-voxel-field-system/diagrams/.gdignore` | 阻止 Godot 导入文档图 | 无输入适配，保留 |
| `demos/core-scenevoxeltile/diagrams/.gdignore` | 阻止 Godot 导入文档图 | 无输入适配，保留 |
| `demos/placement-autoobject-probe-prefilter/diagrams/.gdignore` | 阻止 Godot 导入文档图 | 无输入适配，保留 |
| `demos/placement-voxel-semantic-routing/diagrams/.gdignore` | 阻止 Godot 导入文档图 | 无输入适配，保留 |
| `demos/target-sv-point-cloud-conversion-c/diagrams/.gdignore` | 阻止 Godot 导入文档图 | 无输入适配，保留 |

## 可优先退役的兼容内容

以下项目已有静态零调用或明显被替代信号，但删除前仍需确认外部插件、编辑器 bridge 和反射调用：

- `shaders/prepare_target_read_buffers.glsl`：无 host，当前使用 `pack_target_field.glsl`。
- `SceneVoxelTileCodec.packed_float_field()`、`pack_float_field_bytes()`、`decode_float_field_bytes()`：仓内无调用。
- `VoxelGeneral.expand_scalar_field_to_vec4()`：仓内无调用。
- `DemoUI.find_any_camera()`：仓内无调用。
- `TargetSVSetup.switch_channel()` 及若干 facade getter：仓内无调用；先查外部 editor/plugin 调用。
- `brush_voxel_instances.glsl` 的 `height_span`、`brush_rgba` push，`terrain_collision_volume.glsl` 的 `total_slices`，`voxel_collision_erode.glsl` 的 `core`：已不消费。
- `SceneVoxelTileStore` 返回的两个 `resident_dirty_*_rid`：实际是字符串，随后资源被 `gc_frame()`，仓内无 consumer。
- `SceneVoxelCommitter` 的 facade delegates 不是零调用。必须先决定它是正式公共 facade 还是临时迁移层，不能直接内联删除。

## 不能误删的兼容内容

- `GodotComputeShaderBase` 的 local/global/external RD、owned/borrowed RID、active compute-list 延迟释放。
- `TargetReadBufferBorrow` 的 RID 有效性、同 RenderingDevice、字节数三重校验。
- `PushConstantLayout` 的 std430 对齐和总长 16-byte 对齐。
- `BufferUtils` 中 shader RGBA8 与 semantic RGBA8 两种不同 word order；迁移前不能合并。
- shader 的 dummy binding：只有对应 flag 保证不读时才安全。
- GPU 测试无 RD 的 `SKIP` 与运行时 GPU-only 的 `BLOCKED`，两者不能合并成“成功 fallback”。
- `ReportSchema` 的 contract tier；删键必须与所有 consumer 和契约测试同批修改。

## 优化实施顺序

### 第一阶段：冻结正确性

> **状态：已执行完毕（2026-07-14，工作区未提交）**。8 项全部落地：F1 P0 SPA TargetSV 直读修复（提取 `targetsv_complexity_at/collision_at` 纯函数走 canonical RGBA8/.r8 解码 + 恒过 stub 换真回归）；F2 VoxelPick 同尺寸缓存改 `buffer_update` 覆写 + 同尺寸两次写入测试；F3 TargetSV 三图尺寸/正方形/slice_count 入口硬校验；F4 `decoded_record_count` 非正 stride 守卫 + 测试；F5 runtime dirty delta 事务化（预检已在位，补 GPU 写失败回滚，顺带修出 spawn ID 中毒 bug 与 kill 提交顺序）；F6 五个恒真/失效门禁修复（spa_click_test 恒过断言、golden 基线缺失须 `--approve`、两个 shot 工具重写为桥客户端、dispose 测试量化断言）；F7 四个 resident shader 的 range 容量守卫（首版 `.length()` 触发 `OpArrayLength is not supported yet` 控制台警告，F7b 已换 host 显式容量：residual push 96→112B、stamp 80→96B、probes/apply 复用既有 pad 槽，SPIR-V 反汇编确认 OpArrayLength 0 条）；F8 staging 契约裁决为文档侧修复（profile 容器 staging 概念仍在役），`test_core_demo_contracts` 转绿。
> **门禁 PASS**：fresh `-e` 控制台 0 error/warning（含 0 OpArrayLength）、golden 逐字节 PASS、`run_stamp_only_commit_check` 与改前逐字一致、`run_resident_placement_writeback_check` ok。
> 另：本阶段执行期间用户增设硬规则——**runtime 方式启动 Godot 已被 PreToolUse 钩子禁止**（`.claude/block-godot-runtime.ps1`，仅放行 `-e` 与 `--check-only`），下表"当前验证结果"里的 runtime 测试口径今后须经用户明示许可才可复跑。

1. 修复 SPA TargetSV 直接读取和 VoxelPick 同尺寸缓存。
2. 给 TargetSV 三图尺寸、buffer stride、range capacity 加硬校验。
3. 修复 runtime dirty delta 非事务写入。
4. 修复恒真/失效测试，先让现有契约门禁可信。

### 第二阶段：建立版本化边界

1. `TargetSVManifest` + `TargetSVSnapshot`：格式、stride、尺寸、revision、owner。
2. `TargetReadBufferHandoff`：RID、RD、byte count、format、lifetime、schema version。
3. `Descriptor/Probe/TileDelta/SelectionRecord`：输入处兼容旧键，内部只保留 canonical key。
4. `GpuDeviceContext`：device ownership、capabilities、sync policy。
5. `BufferLayoutRegistry`：生成 GDScript pack/unpack、GLSL 常量和契约测试。

### 第三阶段：拆分生产、调试和迁移路径

1. placement/prefilter/writeback 只保留 resident GPU 路径。
2. CPU decode、readback、旧格式和宽松 pad 进入 debug/migration 模块。
3. editor pick 的 CPU ray fallback 保留，但统一记录 `pick_backend` 并做一致性测试。
4. demo 不再拥有独立 descriptor/profile/TargetSV 生命周期，改消费服务接口。

### 第四阶段：删除历史分支

1. 给旧 alias/schema 增加命中计数。
2. 迁移并重存资源，更新 scene metadata 和文档契约。
3. 删除零调用 helper、orphan shader、死 push、虚假 RID handoff。
4. 合并重复 shader compile、Markdown contract、worktree cleanup 和 screenshot harness。

## 新增计划：二维 volume 存储改制为三维稀疏存储（含旧内容与兼容清理）

用户拍板（2026-07-14）：当前二维 volume 存储方式全部改为三维稀疏存储，并清理旧内容和兼容层。本计划基于 3 个只读测绘代理对当前工作区的逐行核实（committer 989 行 / field_builder 971 行 / tile_store 2149 行 / codec 587 行全读 + 消费方全仓 grep），所有 file:line 以测绘时点为准，执行时按函数名重定位。

### 实测现状（计划依据）

- **生产热路径已全 3D**：SPA/prefilter/VPG/gpu_runtime 全部读常驻 field 对 RID（complexity RGBA8-in-u32 + collision unorm8-in-u32，每体素 4B，resident-or-error）；`committer._volume` 在生产**恒为空**（`build_voxel_volume` 全仓仅 `spa_test`/`test_markdown_contracts` 调用，交互 demo 从不构建），`commit_scene_voxels`/`_rebuild_sv`/`get_sv` 在生产 committer 上因 `_volume.is_empty()` 门 no-op。
- **2D 存储 = 测试/检视专用形态**：`_volume`{`slices:Array[R32 Image]` 逐切片镜像、`slice_meta`、`scene_voxels` 2D 键投影、`collision` 字符串键 cell map、terrain 2D 副本} + occupancy RGBAH 四通道图及其整条盖章/投影链。活消费面仅 `query_voxel`/`get_scene_voxel(s)` 点击 HUD（demo 中因 volume 恒空实际只吐零值）与 ISWS 记录的 2D 键契约。
- **3D 设施在位**：tile 系统 3D 原生（4×4×4、init/reduce/compact/object_ref_update 四 shader 链、3D 脏机制、数值 object refs）；`SvFieldScatter` 稀疏散射；CPU 盖章入口已有 3D 直写半边（`_queue_sv_field_record`→scatter）；terrain 2D 底图→3D u32 种子 pass。
- **稀疏化地基现成**：`compact_scene_voxel_tile_summaries.glsl:33-45` 已有 occupied/empty 判定 + `atomicAdd` 紧凑流 `{tile_index,counts,minmax}`；reduce 有 per-voxel epsilon 门；record/summary 上传缓冲天然只含 touched tiles。**当前稠密的**：field 对本体（voxel_count×4B×2）、object-ref 表、reduce summary 缓冲。
- **三张脏表并存**（审计"旧 2D/new 3D 双写"的真身）：A=`_sv_dirty_tiles`（2D 逐 slice 8px 像素瓦片，**write-only 零读者**）、B=3D tile 脏（真身）、C=`_sv_dirty_rects`（2D 矩形，仅 `build_voxel_volume` 自回放，且其唯一 producer `_mark_sv_rect_dirty` 外部零调用）。

### 阶段 V1 — 退役 2D 存储（清理为主）

> **状态：已执行完毕（2026-07-14，工作区未提交）**。S1/S2/S4/S5 四代理 + 编排终扫全部落地：`_volume` 字典与 occupancy/slices 整条机器删除；A/C 两张 2D 脏表及 B→A 反向同步删除；五处 `_volume.is_empty()` 门换 `_grid_ready()`+buffer 就绪；**地形种子已重接**（committer 级底图+grid_size，分辨率不匹配走保留的最小版 resample）；盖章入口=ISWS 簿记+CPU 圆盘 3D 散射记录（旧 2D kernel 等价）+旧键兼容换算（命中计数）；collision 摘要口径改从 ISWS 记录集重建（排除语义不变）；查询面（query_voxel/get_scene_voxel(s)）签名不变、实现改常驻 buffer 单点/全量回读；ISWS 记录 additive 双写显式 `voxel_min/voxel_max`（半开区间，与 tile_store 显式分支逐位一致）；4 个 2D shader（stamp_rgba_channel_disc/occupancy_slice_image/stamp_collect_voxel_disc_3d/sample_r32_pixel）物理删除，resample_collision_field 留最小版；孤儿 `scene_voxel_volume_channels.gd`、专测 `test_stamp_collect_voxel_disc.gd` 删除；spa_test A 段/markdown 契约段/tile debug demo 改写为 3D 口径。12 文件 parse gate 终扫 0 失败。
> **G-V1 门禁 PASS**：fresh `-e` 控制台零 error/warning；golden 逐字节 PASS（评分链不受影响实证）；`run_stamp_only_commit_check` ok（A 段新口径 scattered=13，B 段 field_max=0.70/brush/feedback 与旧值一致）；`run_resident_placement_writeback_check` 逐字一致；scenevoxeltile demo 场景干净加载。
> **行为提升（预期，勿当回归）**：生产路径 `commit_scene_voxels`/`get_sv` 从 2D 门 no-op 变为真正执行 flush+`_rebuild_sv`；地形基底碰撞在生产真正种入常驻场（旧代码因 `_volume` 恒空而恒全零）；tile debug HUD Grid/tick 从恒零变真实值。
> **遗留（V2/V3 接手）**：`scene_voxel.gd` 的 `stamped()/stamped_template()` 被 V1 孤儿化（migration 新增，0 调用方）；`resident_dirty_tile_worklist/flag_capacity` 两个 stats 键零消费者；`scene-voxel-field-system.md`/`scenevoxeltile.md` 的 2D 描述段待 V3 改写（:289 已删测试命令行已修）；S2 换算等价条件（grid_size.x==\_base_res==volume_xz_resolution）已记入 helper docstring。

| 代理 | 任务 | 关键点 |
| --- | --- | --- |
| V1a | 删三张脏表中的 A+C：`_sv_dirty_tiles`、B→A 反向同步 `_mark_legacy_sv_tiles_for_scene_voxel_tile`、codec `sv_tile_key`/`sv_tile_bounds`、committer `_mark_sv_tile_dirty`/`_mark_sv_rect_dirty` 族 | producers（committer:325-327,451、field_builder:628）改直调 3D touch API；C 的消费只在 `build_voxel_volume` 内，随 V1d 走 |
| V1b | `_volume.is_empty()` 门全部换成 grid_size/常驻 buffer 就绪门：`commit_scene_voxels`(:667)、`_rebuild_sv`(:332)、`get_sv`(:884)、`_flush_pending_sv_field_records`(:480，散射维度 `_volume.xz_res/total_slices`→`grid_size`)、tile_store `_resident_field_voxel_count`(:620) | ⚠ **`_seed_collision_field_base_bytes`(:699) 必须重接 committer 级 `_terrain_base_collision_field`+grid_size**——现状 volume 空即返回全零种子，直接换门不重接会丢地形碰撞 |
| V1c | 查询面 3D 化：`query_voxel`/`get_scene_voxel(s)` 改读常驻 field 对（单体素 offset `buffer_get_data` 或复用 `readback_sv_field_debug_snapshot` 缓存），`scene_voxels` 2D 投影 dict 退役 | 消费方仅 spa_interactive_demo 点击 HUD（:1771-1801）+ 2 个测试；ISWS 记录集保留（可重放输入），投影改按需回读 |
| V1d | 删 2D 机器：occupancy 图+`_stamp_occupancy_channel`+`stamp_rgba_channel_disc.glsl`+`occupancy_slice_image.glsl`；`_stamp_collect_voxel_disc_gpu` slices 镜像往返+`stamp_collect_voxel_disc_3d.glsl`+`sample_r32_pixel.glsl`；`resample_collision_field.glsl`（terrain 缩放并入 3D 种子 pass 或留最小版）；`build_voxel_volume`/`configure_scene_voxel_grid`/`_apply_volume_grid_metadata`；死码清单（`volume_pixel_to_world`、codec legacy float 场族、`pack_ref_hash_bytes`、str 化已 GC 的假 resident RID、`_sv` 恒空 CPU 场键、`_sv[scene_voxel_tile_summary_gpu_rid]` 无读者发布） | CPU 盖章入口瘦身为：ISWS 记录簿记 + `_queue_sv_field_record` 3D 散射（已有）；`spa_test`/`test_markdown_contracts` 的 2D 段同批改写 |
| V1e | 记录契约 3D 化：ISWS 的 `base_pixel`/`radius_px`/`slice_indices`/`volume_xz_resolution` → 显式 `voxel_min`/`voxel_max`(Vector3i)（`_scene_voxel_tile_bounds_from_record`:256 已优先吃显式 3D bounds）；`auto_object` 生产端、`voxel_fixtures`、消费测试同批迁移 | 兼容期按审计"输入边界转换一次"原则：committer 入口留一次旧键→3D 换算，命中计数，后续删除 |
| G-V1 | 门禁：fresh `-e` 零错误 + 桥 golden（评分链不该受影响，逐字 PASS）+ `run_stamp_only_commit_check`（⚠ 其 A 段走 `build_voxel_volume`，须随 V1d 同批改写为 3D 断言）+ tile debug demo 目检 | — |

### 阶段 V2 — 常驻 field 对页式稀疏化（真·稀疏存储）

> **前置状态（2026-07-14）**：① tile-index 布局统一**已完成**——摘要子系统（reduce encode + store CPU decode）翻转为 canonical z-inner `x+gx*(z+gz*y)`，decode 改用 codec 现成 `tile_coord_from_index`，encode→decode 全链路（reduce→init/compact 直传→CPU 解码→tile_key 落表）核实闭合，resident 记录序 buffer 确认不共享槽位未动；门禁 PASS（golden 逐字节、stamp/writeback 与 V1 基线逐字一致、tile demo 干净、控制台零警告）。原冗余审计"双 tile-summary 布局有意分工"冻结项就此解除。
> ② **收益测量发现关键事实**：committer 生产初始化 `grid_size=(base_res, 1, base_res)`——**当前 committed SV 是 Y=1 扁平网格**（`configure_scene_voxel_grid` 只有测试调用；placement 评分/TargetSV 侧是 3D，测试用 Y=4/8）。Y=1 下：collision 场被 terrain 种子铺满全 XZ → 页式稀疏化**零收益**；complexity 场仅 XZ 稀疏 → 半场收益。**V2 全价值成立的条件是 committed SV 升为 3D 网格**（与 TargetSV/3D 锚点/3D stamp 管线对齐——管线本身已支持）。
> **待拍板（V2 实施的先决）**：committed SV 维持 Y=1（→ V2 按计划自设的 <2× 门槛止步或仅稀疏 complexity 半场）vs 升 3D（→ V2 全价值；需定 Y 深度来源，建议对齐 TargetSV slice_count）。

- **目标形态**：tile 目录（按 tile-grid 稠密的 u32 表：`tile_index → page_slot` 或 EMPTY 哨兵）+ 页池（每页 4×4×4×4B，complexity/collision 各一池或合池）；occupied 判定复用 compact 链既有语义。
- **读方改造**（score/collect/pick/prefilter pack/compose/display/erode/merge/种子 等全部线性寻址 shader）：`voxel_index` → `directory[tile_index]` 间接寻址 + 页内偏移；miss = 空值语义（complexity 0 / collision 0）。
- **写方改造**（stamp_asset_voxels/scatter/merge/terrain 种子）：写前保证页存在。首选 **CPU 预分配**——写方 dispatch 前按 stamp bounds/dirty tiles/记录 bounds 展开预留页（信息现成），避免 GPU 侧原子分配的复杂度；GPU 原子按需分配（CAS 目录+页计数）列为备选。
- **设计决策点（执行前须拍板）**：① terrain base 碰撞种子几乎铺满底层 slices，并入稀疏页会让底层页全 occupied——评估 terrain base 单独留稠密薄场（XZ×少量 slice）vs 接受底层页常驻；② BrushSV/BlendSV 对是否同布局稀疏化（compose/score 读两族场，布局不一致则 shader 要走两套寻址）——建议首版仅稀疏 committed 对、Brush/Blend 保持稠密，compose 侧混合寻址；③ EMPTY 哨兵与 dual_commit 双写（stamp 的 commit 目标页也须预留）。
- **收益口径**：稠密现状 256³ = 两场共 128MB 常驻；稀疏后 = 目录（tile 数×4B）+ occupied 页×256B×2。执行前先用 compact 链实测典型场景 occupied tile 比例，收益低于 2× 则本阶段止步（记录数据后再议）。
- G-V2：golden 前后逐字 PASS（值语义不变是硬承诺）+ 三 check + 显示 demo 目检 + VRAM 前后实测。

### 阶段 V3 — 兼容口径与文档收尾

> **状态：已执行完毕（2026-07-14，工作区未提交）**。A 口径收敛：`"target_collision_format"` 契约值全仓（7 文件）`r8_packed_u32`→`unorm8_u32`（复核无逻辑派发、golden 基线不含旧值）；state_chain 三契约构造器的 collision 输出格式/stride 标签对齐 u32 实况（修正与同 dict byte_count 的自相矛盾）；committer/store 双份 SCENE_VOXEL_TILE_* 常量收敛 codec 单源（单层 re-export，SPA handoff 等值校验同源恒真）；零消费 stats 键与 `world_to_volume_pixel` 删除。B 孤儿：`scene_voxel.gd` 的 `stamped()/stamped_template()` + 孤儿 preload 删除。C 文档：两个 demo md 的 2D 时代描述全部改写为 V1 后现实（21 必需词/24 禁词契约按构造满足，Node 脚本模拟契约测试两轮全过）；`core-sv-anchor-collection.tscn` focus metadata 改为单门现状。
> **门禁 PASS**：golden 逐字节、stamp/writeback 检查与 V1 基线逐字一致、fresh `-e` 控制台零 error/warning。
> **V2 最终裁决（用户拍板 2026-07-14）**：页式稀疏化**挂起**——实测 committed SV 生产网格 Y=1 扁平（collision 被 terrain 铺满零收益）且当前尺度稠密对仅 ~8MB，不达收益门槛。**重启条件**：世界尺度显著增大（≥512² XZ 或 committed SV 升 3D 深 Y）时重新评估；届时 tile-index 已统一、golden 网就位，V2 设计（目录+页池+CPU 预分配）按本节原文执行。
> **小额遗留**：`VoxelGeneral.world_to_volume_pixel`（committer 包装删除后 0 调用方，utils 文件不在 V3 独占范围）；`core-sv-anchor-collection.tscn:32-34` Acceptance Label3D 文本仍含旧门措辞（仅显示文案）。

- codec R8 紧凑（1B/体素，磁盘/VPG 输出投影口径）与 u32（4B/体素，常驻口径）双口径显式化：R8 只保留为磁盘/回读边界格式，`voxel_placement_state_chain` 契约标签与 `"target_collision_format"` 值（批次 C 遗留）同批改。
- committer/store 双份 buffer 名/stride/format 常量收敛单份（SPA handoff 等值校验随之只对一处）。
- 文档与场景 metadata：`scene-voxel-field-system.md:57` "盖章 slices+投影" 描述改写；`core-sv-anchor-collection.tscn:11` 陈旧 support 门 metadata 清理；`test_stamp_collect_voxel_disc` 随 kernel 删除。

### 与既有工作的关系

- V1 不依赖审计第二阶段（版本化边界），可先行；V2 依赖 V1 与 golden 基线可信（已具备）。
- 审计索引中 committer facade "正式 facade 还是迁移层"的悬而未决（行 85）在 V1 中顺势裁决：保留的委托面即正式 facade，2D 委托随 2D 删除。
- 本计划的删除清单与"可优先退役的兼容内容"一节部分重叠（legacy float 场族、假 resident RID），以本计划批次执行为准，勿双报。

## 当前验证结果

| 验证 | 结果 | 边界 |
| --- | --- | --- |
| 非 headless Vulkan `tools/verify_shader_compile.gd` | 38/38 shader 编译通过 | 只证明 SPIR-V 编译；不证明 workgroup portability、RID ownership 或 buffer 内容正确 |
| 非 headless Vulkan `tools/test_target_sv_buffer_decode.gd` | 通过 | 覆盖 canonical/legacy decode、resident borrow/block；未覆盖 SPA 直接 record decoder |
| 非 headless Vulkan `tools/test_voxel_pick_gpu.gd` | 通过 | 只调用一次 TargetSV；未覆盖同尺寸内容刷新 |
| headless `tools/test_buffer_utils.gd` | 通过 | 未覆盖 `byte_stride<=0` |
| headless `tools/verify_glsl_gen_blocks.gd` | 12 项通过 | 验证当前生成块文本，不验证所有 host binding |
| headless `tools/test_markdown_contracts.gd` | 通过，3 个 GPU 子项 SKIP | SKIP 符合 headless 规则 |
| headless `tools/test_core_demo_contracts.gd` | 失败 | `_assert_runtime_plan_gpu_first_boundaries()` 要求的 `staging` 文案缺失；需决定修文档还是更新冻结契约 |

## 验收门槛

- 每个 legacy adapter 都有明确 owner、输入版本、命中统计和删除条件。
- 生产路径不再出现未声明的 `Dictionary.get(..., fallback)` 链。
- 每个 GPU buffer 在 host 和 shader 两端都有类型、stride、容量和 ownership。
- 每个 shader 的 workgroup 在目标最低设备能力内，dispatch 和边界测试覆盖非整倍尺寸。
- GPU-only 失败不会返回看似有效的空图、零场或旧值。
- 纯契约测试 headless 通过；RenderingDevice 测试在真实 Vulkan 下通过；SKIP、PASS、BLOCKED 三种结果不混用。
