# 项目优化方案:去冗余清理计划

基于 2026-08-14 对全仓的四路只读扫描(shader 引用链、脚本可达性、跨文件重复代码、文档与资源时效)与人工复核。本文只收录**证据充分**的清理项,每条附 `文件:行号` 证据;行号为扫描日快照,执行时以符号名复核。扫描时工作区位于 `refactor/compute-pass-toolkit` 分支且有未提交改动,所有清理**尚未执行**。

## 扫描范围与总体结论

| 维度 | 数量 |
| --- | --- |
| GDScript | 108 个 `.gd`,50,659 行 |
| Compute/着色器 | 49 个 `.glsl` + 1 个 `.gdshader` |
| 文档 | `doc/` 18 个 `.md` + 根目录 5 个 `.md` |
| 元数据 | 109 个 `.uid`(孤儿 0)、55 个 `.import`(孤儿 1) |

总体结论:**代码本体的去重程度已经很高**,大头冗余不在代码而在文档与本地磁盘。

- shader 层零孤儿:50 个全部有真实加载链;孤儿 `.import` 在 shaders/ 为 0。
- 代码死角极少:1 个孤儿脚本、6 个零调用私有函数、4 个零调用别名;**没有**大段注释掉的死代码,**没有** `if false` 类废弃开关。
- 唯一系统性代码冗余:软重载修复样板(`_repair_soft_reloaded_members` 族),12+4 个文件共 246 行手写类型断言 + 129 处逐字相同的尾注释。
- 最大文档冗余是**反向孤儿**——文档引用已删除的符号:根 `README.md` 3 处、`doc/` 内 10 条失效路径、6 个失效类名、20+ 处失效函数引用、4 个被索引但不存在的文档。
- 本地磁盘垃圾约 55 MB(`backup/` 40 MB、`_shots/` 4.7 MB、`*.TMP` 9.9 MB),git 跟踪冗余约 6.2 MB(6 个零引用 `.raw` + 过期计划副本)。
- git 引用:6 个已全部合入 `main` 的历史分支,本地与远端各一份。

## 安全边界与验证门禁

- **时序**:当前分支有 12 个未提交修改 + 1 个新 shader。清理须等这批工作落地(或明确搁置)后,单开 `chore/redundancy-cleanup` 分支执行,禁止混入进行中的功能分支。
- **每批验证协议**(运行时启动被项目规则禁止,以下是仅有的门禁):
  1. parse gate:对每个改动的 `.gd` 跑 `--check-only --script`;**退出码与 stdout 双判**——依赖文件里的 warning-as-error 不进退出码,只出现在 stdout。
  2. `-e` 门禁:先关闭本项目现有编辑器进程(按命令行过滤,勿伤其他项目),删 stale lock,再 fresh 启动;通过条件 = 控制台无脚本错误 + 桥 `127.0.0.1:6800` 应答 ping。
  3. 涉码批次追加 **golden A/B**:清理前在同一工作区跑 `tools/golden_snapshot_check.js` 存结果 A,清理后跑 B,要求 A=B。工作区 golden 与仓库基线本就不同步,绝对红绿不可用,只看差分。
- **归档优先**:`_archive/` 已在 `.gitignore`。跟踪文件走 `git rm`(历史可找回);非跟踪文件先挪 `_archive/` 观察一轮再删。
- **不自动执行**:删远端分支(外发操作)、动用户本地数据(`backup/`、`.hip`)——见"待用户裁决"。

## 明确不清理的项(负面清单)

扫描证实以下对象**不是**冗余,本轮及后续都不应清理:

| 对象 | 保留理由 |
| --- | --- |
| `shaders/` 全部 50 个 | 零孤儿;11 个 `rdg_*.glsl` 是 `--rdg-selftest` 自检夹具,文件头自标非生产,唯一消费方 `scripts/checks/rdg_selftest.gd` |
| 多态覆写族(`domain_key`/`revision`/`display_node_name` 等 17 族) | `PickableDomain` 继承树的接口契约,同名是多态点不是重复 |
| `scene_placement_actor.gd:2625-2732` 门面块(49 条同行转发) | 块首注释声明的架构边界;逐条 null 回退值类型各异,表驱动化会退化成 Variant |
| SSOT 公式(`dispatch_groups_*`、`align16`、`encode_rgb8` 等)、防御性拷贝 getter(`duplicate(true)` 族)、`is_*`/`has_*` 谓词 | 内联等于把公式复制到调用点/外泄内部可变引用 |
| `compute_kernel.gd:92`/`rdg_builder.gd:70`/`rdg_pool.gd:59` 的 `owner()` 三处同式 | GDScript 无泛型,抽取后返回类型退化为 `Object` |
| `buffer_descriptor.gd:277` 的 `voxel_index` 转发 | 已是正确方向的收敛结果,注释解释了为何不反向合并(热路径比较器) |
| demo 专用脚本(`fly_camera.gd`、`demo_ui.gd`、`placed_instance_display.gd`、`slot_identity_check.gd`) | demo 场景是其设计挂载点 |
| `volume-display-domain-audit.md`(根) | 刻意保留,`doc/README.md` 以相对链接引用 |
| `mempalace.md`(根,未跟踪) | 项目约定保留(本地记忆文件) |
| `landscape/TargetSV_PT.bgeo.sc` | `doc/target-sv-point-cloud-conversion.md:3` 记录的外部源数据存档 |
| `demos/.../spa_selection_mode_transition.svg` | `doc/README.md` 已自标"历史产物",保留是有意决定 |
| `.godot/` 48 MB 缓存 | 可再生,清理无净收益 |
| `scripts/` 根层平铺 53 个文件 | 重排会牵动全仓 preload 路径,高扰动低收益,本轮不做 |

## P0 — 必须处理(正确性/协作)

### P0-1 `textures/` 高度图加载脆弱态 + 6 个零引用 `.raw`

`scripts/target_sv_fbx_import_service.gd:435` 用 `load(path) as Texture2D` 加载 `:59` 硬编码的 `res://textures/scene_height_0_1.png`,但 `textures/` 整目录被 `.gdignore` 屏蔽——资源系统看不见该目录,这条链依赖"加 `.gdignore` 之前遗留的过期 `.import` + `.godot/imported` 缓存"才能工作,缓存一失效导入链就断。

- 修复(推荐):把 `scene_height_0_1.png` 迁至 `assets/target_sv/`(TargetSV 输入的既有归属目录,无 `.gdignore`),同步 `:59` 常量与 `:42` 注释。备选:`_load_height_image` 改 `Image.load_from_file()`——该函数拿到 Texture2D 后立即 `get_image()`,本就只要 Image。
- 顺带:删除 6 个零引用 `.raw`(共 6 MB,`height_normal`/`object_depth`/`object_normal`/`scene_depth`/`scene_normal`/`target_height`,均为已删除的旧 cliff_generator 架构残留),随后 `textures/` 目录即可整体退役。
- 验证:`-e` 启动后经桥触发 TargetSV FBX 导入链,确认无 `height_texture_unavailable`。

### P0-2 孤儿 `geo/cliff_02.import`

`source_file` 指向 `res://geo/cliff_02-冲突-又上医院_Win10.FBX`(同步冲突文件,已不存在),与正常的 `cliff_02.FBX.import` 并存,纯误导残留。`git rm geo/cliff_02.import`。

### P0-3 根 `README.md` 点选段描述已删组件

`README.md:47-48` 与 `:108` 仍把 `VoxelPickGPU`(`scripts/voxel_pick_gpu.gd` + `shaders/pick_scene_voxel.glsl`)与 `UnifiedPickGPU`(`scripts/unified_pick_gpu.gd` + `shaders/pick_unified.glsl`)描述为现存组件——四个文件均已于 2026-08-10 删除(删除记录见 `scripts/blend_sv_volume.gd:20`、`scripts/checks/glsl_gen_block_checks.gd:38-40`)。按现状重写为 `pick_id` ID 拾取路径。

### P0-4 `doc/README.md` 索引 4 个不存在的文档

"Plans"/"Demo And Test Contracts" 表列出但 `doc/` 下不存在:`ui-click-test-plan.md`、`gpu-direct-rendering-plan.md`、`project-simplification-plan.md`、`unified-picking-plan.md`。其中 `unified-picking-plan.md` 还被 `doc/auto-volume-base-class-plan.md:13` 当上游文档、以《点选统一》简称贯穿 §10.4/§11——整条引用链断裂。处理:索引行删除或标注"已删";`auto-volume-base-class-plan.md` 内的引用改写为自包含说明,**不重建已删文档**。

### P0-5 `voxel_multimesh_writer_gpu.gd:82` 的 `last_reason()` 缺软重载修复

三份 `last_reason()` 中唯此份是不带类型修复的 1 行版(对照 `auto_object_instance_renderer.gd:248`、`utils/placed_instance_display.gd:52` 的字节级相同实现)——软重载后会返回 Nil 而非空串,是审计顺带发现的真 bug,补齐对齐三者语义。

## P1 — 建议处理(可维护性)

### P1-1 零风险文件清理

| 对象 | 大小 | 操作 | 证据 |
| --- | --- | --- | --- |
| `AutoVolume公用体积基类计划.md`(根) | 92 KB | `git rm` | `doc/auto-volume-base-class-plan.md` 的过期全文副本:doc 版多 8 条 2026-08-09/10 进度行;根版仍用已改名符号(`DATA_PICK_MODE_PREFERENCE`→现名 `DATA_PICK_DOMAIN_PREFERENCE`)、称 `configure_grid` 仍在(实际全仓 0 命中,doc 版已注明 2026-08-10 删除) |
| `scenes/asset-overview/baked_descriptors/*.tres~RF*.TMP` ×5 | 9.9 MB | 本地删除 | Godot 保存中断残留(08-13),已被 `.gitignore` 的 `*.tmp` 覆盖 |
| `demos/core-sv-anchor-collection/`(空目录) | — | 本地删除 | 目录内无任何文件;`doc/sv-anchor-collection.md` 从不引用它 |
| `scripts/scene_voxel_target.gd` + `.uid` | 20 行 | `git rm` | 类名 `SceneVoxelTarget` 与 4 个公开符号全仓零引用;`FLAG_ROUTING`/`FLAG_FEEDBACK` 已被 `scene_voxel_tile_codec.gd:44,46` 独立重定义并实际使用 |
| 本地已合并分支 ×6 | — | `git branch -d` | `202605017`/`20260511`/`20260514`/`20260523`/`20260609`/`refactor/committer-split-stage1` 均已验证 `--merged main` |

删除根计划副本前的复核命令(应显示 doc 版为严格超集):

```powershell
git diff --no-index "AutoVolume公用体积基类计划.md" "doc/auto-volume-base-class-plan.md"
```

### P1-2 死代码删除(零调用方,私有/无外部消费者)

| 位置 | 符号 | 备注 |
| --- | --- | --- |
| `scripts/scene_voxel_tile_store.gd:271` | `_scene_voxel_tile_normalized_bounds` | 调用点已随重构消失,仅存 `.git` 历史 |
| `demos/placement-score-3d/volume_score_demo.gd:3038` | `_selected_anchor_sample_bounds` | 同上 |
| `demos/placement-score-3d/volume_score_demo.gd:1954` | `_status` | 状态字典构造器,无消费者 |
| `scripts/checks/placement_stage_env.gd:780` | `_terrain_base_collision_image` | 测试夹具遗留 |
| `scripts/utils/buffer_descriptor.gd:505` | `_aos_field_names` | AOS 字段名列举,无消费者 |
| `scripts/spa_selection_host.gd:645` | `_vec3_to_array`(static) | 序列化助手,无消费者 |
| `volume_score_demo.gd:1819,1823` | `get_anchor_world_positions` / `get_anchor_marker_radius` | 零调用公开别名(私有版各有 10/3 个内部调用方,保留) |
| `scripts/scene_placement_actor.gd:1802,1806` | `get_editor_overlay_text` 并入 `get_selection_overlay_text` | 插件按 `get_selection_overlay_text` 探测(`meshfill_editor_plugin.gd:245-256` 4 处);实现体反而挂在零调用的旧名下,方向倒置 |
| `scripts/checks/rdg_selftest.gd:1198` | `_g3` 内联 | 单调用点对称性产物(`_g1` 51 调用、`_g2` 14 调用保留) |

### P1-3 文档失效引用修复

处理原则:引用已删符号的句子**改写为现状或删除**;历史决策记录保留但补"已删"标注;不重建已删文档。`doc/auto-volume-base-class-plan.md` 中标注为"已删除/未落地"的条目属预期记录,不改。

失效文件路径(全仓 0 命中):

| 失效路径 | 引用文档 |
| --- | --- |
| `scripts/spa_interaction_host.gd` | `core-scene-placement-actor.md`、`placement-score-3d.md`、`scene-placement-actor.md` |
| `scripts/object_volume_score_gpu.gd` | `placement-score-3d.md` |
| `scripts/checks/autoobject_instance_checks.gd` | `auto-object-instance-emit.md` |
| `shaders/pick_id.gd` | `auto-volume-base-class-plan.md` |
| `shaders/pick_scene_voxel.glsl` | `core-scene-placement-actor.md` |
| `assets/vegetation/leaf_descriptor.tres` | `auto-asset-scripting.md` |
| `tools/test_spa_scene_resident.gd` 等 4 个 `test_*.gd` | `auto-volume-base-class-plan.md`、`core-scene-placement-actor.md`、`doc/README.md`、`target-scene-voxel-projection.md` |

(`tools/` 下已无任何 `test_*.gd`——22 个已于 2026-08-07 删除,所有"跑这个测试"指令均不可执行,一并改写。)

失效类名:

| 类名 | 引用文档 | 现状 |
| --- | --- | --- |
| `SPAInteractionHost` | `core-scene-placement-actor.md`、`placement-score-3d.md` | 现存类是 `SPASelectionHost` |
| `VolumeScore3D` | `placement-score-3d.md` | 实为无 class_name 的 `volume_score_demo.gd` |
| `AutoVoxelRuntimeProfile` | `asset-properties.md`、`auto-object-gpu-runtime-architecture.md` | 现存 `AutoVoxelProfile` / `AutoVoxelRuntimeProfileContainer` |
| `AutoSV`、`SceneVoxelLocal` | `auto-object-gpu-runtime-architecture.md`、`scene-voxel-field-system.md` | 无对应实现 |
| `VoxelPickGPU` / `UnifiedPickGPU` | `core-scene-placement-actor.md` 等 3 份 | 脚本已删(P0-3 同源) |

失效函数引用约 20+ 处,集中在 `scene-voxel-field-system.md`、`scene-voxel-tile.md`、`auto-asset-scripting.md`、`asset-properties.md`、`auto-object-gpu-runtime-architecture.md`、`sv-anchor-collection.md`、`asset-descriptor.md`(典型:`invalidate_sv_tile`、`make_instance_config`、`decode_gpu`、`_voxel_write_spec`)。逐文档过一遍,与该文档其他改写合并提交。

### P1-4 小型重复提取与边界修复

新建任何共享模块前先过 semantic gate(mempalace 概念登记);能落进既有 utils 的不新建。

| 项 | 位置 | 操作 |
| --- | --- | --- |
| `_resolve_scene_placement_actor` 逐字重复 | `pickable_domain.gd:122`、`spa_selection_host.gd:201` | `PickableDomain` 那份静态化,SelectionHost 调它。注意 `:130` 注释记录过一次"零调用静态版被删"——现在出现第二消费者,前提已变,重新静态化不是回退 |
| `_grid_size` 三段回退同构 | `scene_voxel_field_builder.gd:78`、`scene_voxel_tile_store.gd:200` | 提取 static 助手至既有 utils(落点执行时按语义归属定);两处注释记录的是同一条历史 bug("曾静默返回 `Vector3i.ONE`"),副本会让修复只覆盖一半 |
| stage 位测试同体异名 | `asset_descriptor.gd:333`、`auto_voxel_runtime_profile_container.gd:1372` | 移入 `utils/profile_record_schema.gd`(flags 语义 SSOT 已在此) |
| GLSL 生成块协议四处同形 | `auto_object_instance_renderer.gd:784`、`utils/placement_shared_glsl.gd:234`、`utils/profile_arena_layout.gd:299`、`utils/profile_record_schema.gd:551` | 统一助手;3/4 处对未知块名静默返回空串(会把"块名写错"误判成"正文为空"),只有 renderer 那份有 `push_error` 防护——统一补上 |
| `TargetSVSetup` 等价覆写 | `utils/target_sv_setup.gd:82,86` + 两 const | 删除——`display_fill_ratio` 0.72 / `min_cell_height` 0.02 与基类 `pickable_domain.gd:488-489` 默认值完全相同,零行为差异的覆写让读者误以为 TargetSV 有特殊显示参数 |
| `revision()` 逐字重复 | `scene_sv_volume.gd:249`、`svtile_volume.gd:100` | 基类加受保护助手 `_svtile_gpu_revision()`,两域各一行转发;把这条 V3 前的临时耦合收敛到一个可搜索点 |
| 越界调用私有回读口 | `scene_voxel_debug.gd:70-76`(7 处) | 改调公开名 `read_scene_voxel_tile_buffer_bytes`——公开口的注释是"绝不能进每帧循环"的唯一警示位置 |
| `register_assets` 中间跳 | `scene_placement_runtime.gd:605` | SPA 门面直接转发 `register_asset_batch`;公开 API 契约(`doc/scene-placement-actor.md:148`)不变 |
| 跨模块再导出别名 ×3 | `scene_voxel_tile_codec.gd:248`、`auto_asset_factory.gd:21,60` | 内联,调用方直接引用 `VoxelGeneral` / `DemoAssets` 源模块 |

### P1-5 软重载修复样板声明式化(独立专项)

全仓最大的单一代码冗余:`_repair_soft_reloaded_members()` 定义于 12 个文件、`_repair_soft_reloaded_domain_members()` 再加 4 个,共 246 行 `if not (X is T): X = default` 手写断言 + 129 处逐字相同的尾注释。密度前三:`voxel_placement_generator.gd` 37 行、`scene_placement_actor.gd` 31 行、`volume_score_demo.gd` 28 行。

- 动机:软重载保留已有成员值、**新增成员回来是 nil**——任何一条断言漏写都是静默故障(Nil 被 `maxi` 吃成 0、对 Nil 求布尔)。手写样板无法核对"新增成员忘了登记"。
- 方向:新建声明式修复模块(成员表 `REPAIR_SPEC` + 统一 `apply(self, spec)`),把 246 行断言换成每类一张可自动核对的表。属新语义概念,执行前过 semantic gate 登记。
- 风险:触面 16 文件/129 调用点,必须单独批次,逐文件 parse gate + golden A/B,并核对每张 `REPAIR_SPEC` 与类成员声明一致。

> **2026-08-17 执行结论:本项搁置(用户裁定),不改机制。** 原型转换脚本已跑通全 16 文件,
> 结论是这条方向**按原样落地是净负收益**:
>
> 1. **行数不降。** 一条 `if not (X is T): X = d` 换成一行 `["X", T, d],`,几乎 1:1。
>    "246 行收敛为表驱动"这个预期收益不成立。
> 2. **反而丢一层保护。** 表用字符串键走 `set()/get()`:成员名打错时,手写版是**解析错误**
>    (`-e` 门禁当场红),表驱动版是 `set()` 静默空转——恰好把本项要消灭的那类静默故障又
>    请了回来。
> 3. **唯一真收益(自动对账)的成本被低估。** 表是数据,才谈得上拿它与
>    `get_property_list()` 的实际声明双向对账,这确实能答"新增成员忘了登记"。但对账需要一份
>    `exempt` 名单(有意不修的成员)。实测:**仅 8/16 文件就有 254 个声明成员 vs 133 个已
>    修复,缺口 121**;全 16 文件约 200+。每条都要人工判"该豁免还是该登记",判错不是弄脏
>    `-e` 门禁(噪声门禁 = 没有门禁,本仓在 `spa_checks` 上已栽过一次,见
>    `doc/auto-volume-base-class-plan.md` §4 那条)就是静默改行为。这份名单才是本项的真实
>    工作量与真实风险,原计划完全没计入。
> 4. **表也表达不了跨成员连带语义。** 16 份里有 6 份带这类逻辑(`pickable_domain` 的
>    `cache_lost` 连带推回 `_built_revision`、`target_sv_setup` 的 `decoded_lost` 驱动
>    `_ready_ok`、容器的 `staging_lost` 驱动 `_uploaded_revision`、`anchor_volume` 的白名单/
>    区间校验),只能留在 `apply()` 之后 ⇒ 两套机制并存而非一套。
>
> **真正值得做的是那个 121 的缺口本身**——它是数据问题,不是机制问题。下次推进时先静态生成
> "声明了但没登记修复"的完整名单并逐条判真伪(其中可能藏着真的漏登记 = 真 bug),再决定要
> 不要动机制。**机制重构不是找出漏登记成员的前置条件。**

## P2 — 可选优化(本地磁盘/低频)

| 项 | 现状 | 建议 |
| --- | --- | --- |
| `backup/` | 69 个 `AssetDescriptor_bakNN.hip`,40 MB,已 gitignore | 保留最近 10 个,其余删除(用户数据,仅建议) |
| `_shots/` | 19 张调试截图,4.7 MB,已 gitignore | 可再生,清空或挪 `_archive/` |
| `tools/shot_asset_overview.gd` + `scripts/utils/shot_utils.gd` | 已断链:要求的 `Title`/`TestMethod`/`Acceptance` 节点在 `asset-overview.tscn` 不存在,恒输出 FAIL;`find_child("Terrain")` 静默空转;`shot_utils.gd` 唯一消费者就是它 | 一并删除(截图职能已由编辑器桥/外部捕获脚本覆盖) |
| `tools/benchmark_score_timing.gd` | 接口与 `volume_score_demo.gd` 逐一对上(7 个桥方法全部存在) | 保留(经用户许可的运行时场景可用) |
| `tools/verify_shader_compile.gd` | 自洽,不依赖 scripts/ 接口 | 保留(离线 shader 门禁的备选实现) |

## 待用户裁决(不自动执行)

| 项 | 背景 | 待答问题 |
| --- | --- | --- |
| `assets/vegetation/sm_test_leaf_test2_asset.tres` | 零代码引用——`BakedAssetLoader` 只扫 `scenes/asset-overview/baked_descriptors`(`baked_asset_loader.gd:22`),不扫 `assets/vegetation`;仅 `doc/auto-asset-scripting.md` 提及 | 待接线还是已废弃? |
| `demos/core-SPA-scene-placement-actor/diagrams/asset-descriptor-bake-to-score-chain.svg` | 15 张 svg 中唯一零 md 引用 | 补进 `doc/README.md` 索引还是删? |
| 远端 6 个已合并分支 | `git push origin --delete` 属外发操作 | 是否随本地一并清? |
| `H_Doc.hip`(根,197 KB) | 4 个 `.hip` 中唯一约 3 个月未动(磁盘 mtime 2026-05-23),零引用 | 退役归档还是保留? |
| `.codex/` | 与 `.claude/` 各有一份 `semantic_gate_user_prompt.py`,内容已分叉 | Codex 若已停用则整目录退役? |
| `backup/` 保留策略 | 见 P2 | 保留几个? |

## 执行批次

| 批次 | 内容 | 验证 |
| --- | --- | --- |
| 0 前置 | 当前分支落地或明确搁置;开 `chore/redundancy-cleanup`;跑一次 golden 存基线 A | — |
| 1 纯文件清理 | P0-2 + P1-1 全部(TMP/空目录/孤儿 import/根计划副本/本地分支/`scene_voxel_target.gd`) | `-e` 门禁兜底 |
| 2 文档修复 | P0-3、P0-4、P1-3;顺带把本计划登记进 `doc/README.md` 索引 | 相对链接存在性自查 |
| 3 行为性小修 | P0-1(textures 迁移)、P0-5(`last_reason` 补修) | parse gate + `-e` + 桥触发 TargetSV 导入链 |
| 4 死代码与小型提取 | P1-2、P1-4 | parse gate 双判 + `-e` + golden A/B(=批次 0 基线) |
| 5 软重载专项 | P1-5 | 同批次 4,另加逐文件 `REPAIR_SPEC` 与成员声明核对 |
| 6 收尾 | 复扫抽查(孤儿/引用四路各抽 3 项);P2 与待裁决项按用户答复执行 | — |

每批一个或数个独立 commit,消息注明对应本计划的编号(如 `chore(cleanup): P1-1 删除根计划副本`),失败可单批回退。

## 预期收益

| 维度 | 数值 |
| --- | --- |
| git 跟踪体积 | 约 -6.2 MB(6 个 `.raw` 6 MB + 根计划副本 92 KB + 孤儿 import/脚本) |
| 本地磁盘 | 约 -15 MB 立即(`*.TMP` 9.9 + `_shots/` 4.7),`backup/` 视裁决再 -35 MB |
| 代码 | 删除死代码/别名约 150 行;246 行软重载样板收敛为表驱动 |
| 文档 | 修复 10 条失效路径、6 个失效类名、20+ 失效函数引用、4 条断链索引、根 README 3 段 |
| git 引用 | -6 本地分支(远端视裁决再 -6) |
