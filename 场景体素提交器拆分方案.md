# 场景体素提交器（SceneVoxelCommitter）拆分方案

针对 [scene_voxel_committer.gd](scripts/scene_voxel_committer.gd)（9460 行 / 281 函数）的结构性拆分。当前文件是"GPU 生命周期编排 + 已抽出的无状态 codec 之上的包装层 + 三个仍内联的有状态 GPU 子系统"叠在一起。目标是把它分解为单一职责单元，在**不改变任何外部契约**的前提下，把最大文件从 9460 行压到约 2500 行。

拆分延续项目已有范式：无状态逻辑下沉到静态 `RefCounted` sidecar（[scene_voxel_tile_codec.gd](scripts/scene_voxel_tile_codec.gd)、[scene_voxel_commit_payload.gd](scripts/scene_voxel_commit_payload.gd) 已是此类），有状态 GPU 子系统抽成 `extends godot_compute_shader_base.gd` 的组合组件。

## 1. 现状诊断

类声明：`SceneVoxelCommitter extends "res://scripts/godot_compute_shader_base.gd"`。成员状态分两块，中间夹约 7200 行方法。

| 状态区块 | 行号 | 内容 |
| --- | --- | --- |
| GPU 管线状态 | `L86-L187` | 约 30 对 shader/pipeline `RID` + sampler + `occupancy`/`_*_collision_field` 四个 `Image` |
| 数据模型状态 | `L7386-L7538` | `_volume`、写规格、源候选缓冲、committed payload 缓冲、`_sv`、脏瓦片、`_scene_voxel_tile_gpu_*` 全套、object refs、tick |

281 个函数按职责聚成 6 个簇：

| 簇 | 主行号 | 规模(估算) | 自有状态 | 去向 |
| --- | --- | --- | --- | --- |
| ① GPU 生命周期 | `L191-L567` | ~380 | 顶部 `RID` 池 | 留（编排器） |
| ② 网格 + 坐标变换 | `L576-L865` | ~290 | `grid_size`/`grid_origin`/`voxel_size` | 部分 static 下沉 `CommonVoxelSpace` |
| ③ 碰撞/占据场 + 盖章 | `L870-L1741` | ~870 | `occupancy`、`_*_collision_field` | `CollisionFieldBuilder` |
| ④ 场景体素瓦片存储（最大） | `L1925-L4474`、`L6414-L6725`、`L8427-L8945` | ~3300 | `_scene_voxel_tile_gpu_*` 全套 | `SceneVoxelTileStore` |
| ⑤ 源候选 staging + commit/blend | `L4622-L6406` | ~1780 | `_*_source_candidate_*`、`_committed_*_buffer` | `SceneVoxelSourceStaging` |
| ⑥ 中央 rebuild + 公共 build/blend/query | `L6467-L9460` | ~2500 | `_volume`、写规格、tick | 留（公共契约 + 编排器） |

簇④存在大量纯包装方法，真身已在 codec。示例见 [`_decode_scene_voxel_tile_records()`](scripts/scene_voxel_committer.gd#L4466-L4467) 与 [`_pack_scene_voxel_tile_summary_bytes()`](scripts/scene_voxel_committer.gd#L4340-L4341)，均为对 `SceneVoxelTileCodecScript` 的单行转发。

## 2. GDScript 约束与拆分手段

GDScript **没有 partial class**，单个类无法物理跨文件。合规手段只有两种：

| 手段 | 适用 | 代价 |
| --- | --- | --- |
| 静态 sidecar（`RefCounted` + `static func`） | 无状态逻辑（打包/解码/坐标） | 低；项目已在用 |
| 组合（owned 组件，各自持有 `RID`/状态） | 有状态 GPU 子系统 | 中；需迁移状态 + 改写簇间调用为显式参数 |

> ⚠️ 不要用 `extends` 链（`Committer extends TileLayer extends SourceLayer ...`）硬拆。它能减行数，但仍是一个对象、共享 `self` 命名空间会撞名、且无法独立测试——是诱人的陷阱。

基类 [godot_compute_shader_base.gd](scripts/godot_compute_shader_base.gd) 已提供完整 GPU 资源生命周期（`ensure_device` / `track_rid` / `release_rid` / `gc_frame` / `load_compute_shader` / `create_uniform_set` / `dispatch_groups_*` / `dispose`）。每个抽出的组件 `extends` 它，由 committer 借出同一个 `RenderingDevice`：

```gdscript
# committer 拥有设备，组件借用（owns_device=false），共享同一套 GC 纪律
_tile_store.attach_rendering_device(_rd, false)
_source_staging.attach_rendering_device(_rd, false)
_field_builder.attach_rendering_device(_rd, false)
```

> 组件间是 GDScript 对象方法调用，**零 marshalling 成本**（marshalling 只是 GDScript↔C# 的问题）。因此拆分不需要换语言。

## 3. 目标结构

| 文件 | 行数(估算) | 角色 | 来源簇 |
| --- | --- | --- | --- |
| `scripts/scene_voxel_committer.gd` | 9460 → ~2500 | 编排器 + 公共 API + 写规格 + `_rebuild_sv` | ①⑥ |
| `scripts/scene_voxel_tile_store.gd`（新） | ~2800 | `extends ComputeShaderBase` | ④ |
| `scripts/scene_voxel_source_staging.gd`（新） | ~1600 | `extends ComputeShaderBase` | ⑤ |
| `scripts/collision_field_builder.gd`（新） | ~850 | `extends ComputeShaderBase` | ③ |
| `scripts/scene_voxel_tile_codec.gd` | 459 → ~600 | 接收簇④残余无状态打包/解码 | ④ |
| `scripts/scene_voxel_commit_payload.gd` | 288 → ~360 | 接收簇⑤残余无状态打包 | ⑤ |
| `scripts/common_voxel_space.gd` | 7 → ~80 | 接收簇②坐标变换 static | ② |

总行数基本不变（这是**分解**而非删除）。收益是最大文件 9460 → ~2500，且四个单元各自可独立 `--import` 解析、独立跑 `test_*.gd`。

## 4. 分阶段执行

按风险/收益排序，每阶段单独一个 commit。

### 4.1 Stage 0 — 无状态下沉 + 内联纯别名

- **做什么**：把 `L4318-L4475`、`L4622-L4655` 这类纯单行包装内联为对 codec/payload 的直接调用；把仍内联的**无状态**逻辑（[`_pack_scene_voxel_tile_fixed_object_ref_hash_bytes()`](scripts/scene_voxel_committer.gd#L4355) 等的纯打包部分、`_scene_voxel_tile_numeric_object_id*`）迁入 `scene_voxel_tile_codec.gd`；坐标 `*_static` 迁入 `common_voxel_space.gd`。
- **状态迁移**：无。
- **风险**：极低（签名不变或纯删包装）。
- **DoD**：`--headless --import` 解析干净；`test_core_demo_contracts.gd` 通过。
- **净效果**：函数数大幅下降，约 -300~500 行。

### 4.2 Stage 1 — 抽出 `SceneVoxelTileStore`（最大收益）

- **搬什么**：簇④全部（瓦片记录/摘要/脏索引/object-ref/field 六个 GPU 缓冲的创建/复用/上传/reduce/compact，以及 [`try_apply_gpu_autoobject_object_ref_update_pass()`](scripts/scene_voxel_committer.gd#L8472)、[`apply_gpu_autoobject_dirty_delta()`](scripts/scene_voxel_committer.gd#L8668) 系列）。
- **状态迁移**：`L7456-L7532` 整块（`_scene_voxel_tiles`、`_scene_voxel_tile_gpu_*`、`_scene_voxel_tile_object_ref_*`、epoch/revision）+ 对应 shader `RID`。
- **committer 委托**：`ensure_scene_voxel_tile_buffers_uploaded`、`get_scene_voxel_tile_gpu_buffer*`、`mark_scene_voxel_tile_*_dirty`、object-ref 系列 → 转发给 `_tile_store`。
- **为何先做**：[gpu_autoobject_runtime.gd](scripts/gpu_autoobject_runtime.gd) 的 `flush_to_scene_voxel_committer` 正是冲着这套 object-ref / dirty-delta 来的；独立命名等于把该契约显式化。
- **风险**：中（状态最多，但边界最干净）。
- **DoD**：`test_autoobject_probe_prefilter.gd`、`test_target_sv_buffer_decode.gd` 通过。

### 4.3 Stage 2 — 抽出 `SceneVoxelSourceStaging`

- **搬什么**：簇⑤（源流/候选 queue·stage·pack·resolve、committed payload + key-coord 缓冲、blend-commit、collision record merge）。
- **状态迁移**：`L7396-L7454`（`_*_scene_voxel_sources`、`_pending_*_candidates`、`_scene_voxel_source_candidate_*_buffer/capacity/count`、`_committed_scene_voxel_*_buffer*`、resolve/blend summary）。
- **committer 委托**：`stage_pending_scene_voxel_source_candidates_to_resident_buffers`、`get_committed_scene_voxel_*_summary`；[`blend_scene_voxels()`](scripts/scene_voxel_committer.gd#L7670) 内部改为从 `_source_staging` 取 committed buffer `RID` 再喂给 `_tile_store`。
- **风险**：中。
- **DoD**：`test_target_sv_point_cloud_conversion.gd` + SPA demo 冒烟。

### 4.4 Stage 3 — 抽出 `CollisionFieldBuilder`

- **搬什么**：簇③（disc 盖章、collision 场 make/resample/merge、shared field layers、occupancy 盖章、mask import）。
- **状态迁移**：`occupancy`、`_source_collision_field`、`_terrain_base_collision_field`、`_collision_field` + stamp/collision shader `RID`。
- **committer 委托**：`import_mask_channel`、`set_terrain_base_collision_field`、`reset_occupancy`。
- **⚠️ 公共属性兼容**：`occupancy` 被 [main.gd](scripts/main.gd#L1014) 当公共属性直接读。保留属性级转发，外部零改动：

```gdscript
var occupancy: Image:
	get:
		return _field_builder.occupancy
```

- **风险**：中低。
- **DoD**：`main.gd` 重建路径 + `test_stamp_collect_voxel_disc.gd` 通过。

## 5. 顺序与验证纪律

- **顺序**：0 → 1 → 2 → 3，每阶段单独 commit，各自 `--headless --import` 解析检查 + 跑相关 `test_*.gd`（项目有 13 个），用现成测试桩当安全网。
- **device/dispose 顺序**：committer 拥有 `RenderingDevice`，`attach_rendering_device(_rd, false)` 借给三个组件；[`_free_gpu()`](scripts/scene_voxel_committer.gd#L491) 中**先 dispose 子组件再 dispose 自己**；每个组件的 `RID` 由各自基类 GC 释放，避免 use-after-free。
- **GDScript 工具弱**：无可靠跨文件 rename，每步靠机械搬移 + 解析检查 + 测试，不要一次大改。

外部契约（必须保持不变，全部通过 committer 转发）：

| 调用方 | 依赖的方法/属性 |
| --- | --- |
| [main.gd](scripts/main.gd) | `get_sv`、`get_voxel_write_spec(s)`、`apply_voxel_write_spec`、`occupancy`、`import_mask_channel`、`reset_occupancy`、`build_voxel_volume`、`_free_gpu`、`get_voxel_slice_count` |
| [gpu_autoobject_runtime.gd](scripts/gpu_autoobject_runtime.gd) | `apply_gpu_autoobject_dirty_delta(s)`、`try_apply_gpu_autoobject_object_ref_update_pass*` |
| [scene_placement_actor.gd](scripts/scene_placement_actor.gd) | `attach_sv_committer` / `get_sv_committer`（持引用）、`get_scene_voxel_tile_gpu_buffer_summary` |

## 6. 预期结果

- 最大文件 9460 → ~2500 行；委员会类回归"公共契约 + 编排"单一职责。
- 三个 GPU 子系统各自可独立加载/测试/复用。
- 总行数不减（分解非删除），但可维护性质变：单文件解析时间、单元测试粒度、并发改动冲突面全部下降。
- 全程不碰语言选型；这是"项目庞大"的结构性解法。

## 7. 风险与待确认

- 各文件行数为按行号区间的**估算**，实际迁移后需复测。
- 簇内仍有少量包装方法保留与否需逐个判断："故意保留的 facade"（稳定公共名）应保留，纯内部别名可内联。
- 簇间数据流（[`_rebuild_sv()`](scripts/scene_voxel_committer.gd#L6725)、`blend_scene_voxels()`）当前靠共享 `self` 状态隐式串联；抽出后须改为显式方法参数，这是主要改写点与潜在 bug 来源，须逐阶段用测试桩兜底。
- 簇④的 reduce（`L6414-L6725`）与 object-ref 更新（`L8427-L8945`）物理上离主簇较远，迁移时注意一并归入 `SceneVoxelTileStore`，勿遗漏。
