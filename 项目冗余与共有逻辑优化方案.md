# 项目冗余与共有逻辑优化方案

本方案来自 2026-07-10 的全项目双波审计（135 个代理，113 条发现逐条对抗核实）：**84 条确认值得做**，合计约 5,000 行冗余代码（约占代码量 15%）+ 约 5.7MB 孤儿二进制；28 条"重复属实但不值得动"；仅 1 条误报。所有发现均已过既有决策 keep-list（别名清扫保留项、RD 收敛例外、push 迁移 poor-fit、committer facade 桩等未重报）。

审计基线 = 当日 `refactor/compute-pass-toolkit` 分支工作区。当日评分链另有并发改动（per-voxel debug buffer、`tile_topk` CPU 回读删除），**执行涉及 score/placement 的条目前先快速复核行号与现状**。

> **执行进度（2026-07-11 复核 + 收尾）**：批次 1 ✅（`2ba7163` + `7b629ff`，拍板项 1/3 已随之消化——output 瘦身至仅 `results_to_world_gpu`、factory 瘦身至 5 个委托 + 教程同步）；批次 4 ✅（`3f040a2`）；批次 2 核心 ✅（`e340696` + Debug 方案落地顺带收敛 B3/B4/B5/B6，B2 显示栈 ✅，尾巴 B1 borrow 核心 + B7 results→world 骨架 ✅ `373dde6`；剩 demo/test 侧长尾：unshaded 材质 ×8、线框盒 ×2、`_scan_descriptor_paths`、`get_source_mesh` ×2、probe rgba8 ×2、R8/RGBA8 codec、spa_test dispose-fail 50+、voxel_display ×3、EvalResult 清零 ×3、plugin StyleBox `*_all`——低价值机会主义）；批次 3 ✅（tile-id codec `e1c6b4d`、yaw 统一 `f116e3e`、pack/expand 收敛 `9750dd2`——expand 并入 pack 的 `use_summary_filter` 开关）。拍板项 2 已由用户拍板**删除**并完成（`3fc9516`，−714 行：控制面 + 队列专属助手闭包 + 零读者广告键 + 架构文档/SVG/README 同步；活路径 spawn_batch ×2 与单对象 API 未动）。死常量残渣与孤儿 .uid 已清（`a0b18c3`）。**冗余方案至此全部执行完毕**（仅剩"确认存在但不建议做"名单与 demo/test 长尾）。
>
> **自监督循环（2026-07-12 起，每 10min，不 commit）**：
> - **轮 1**：长尾对账——unshaded 材质、跨文件线框盒对、`_scan_descriptor_paths`、EvalResult 清零 ×3、plugin StyleBox `*_all` 五项经复核**早已随既往批次完成**（清单滞后于现实）；`get_source_mesh` ×2 本轮收敛（`AutoAssetFactory.resolve_cached_source_mesh` 共享"解析+缓存回写"收尾，两调用方 9→4 行，--check-only ×3 过）。剩余待做：probe rgba8 ×2 / pad-float-array ×3、R8/RGBA8 codec（target_scene_voxel_generator）、spa_test dispose-fail、voxel_display MultiMesh ×3、Debug 方案的 `debug_read_*` 散点开关归并与 writeback merge 后再发射硬门、score_sum=0.0 调查；恒定 provenance 键整删属删除性决定**待用户拍板，循环跳过**。
> - **轮 2**：pad-float-array ×3 收敛——`VoxelGeneral.pad_float_array`（只补不裁；SPA 版的 `maxi(expected,1)` 经证为死分支，语义无损）+ `VoxelGeneral.fit_float_array`（精确重设，原 VPG 语义），prefilter/SPA/VPG 三处私有实现删除、6 个调用点改走共享，--check-only ×4 过。对账：`target_scene_voxel_generator` 的 R8/RGBA8 byte-count 帮助函数**已是 SceneVoxelTileCodec 委托形态**（既往批次完成，清单滞后）。待做队列不变（probe rgba8、spa_test dispose-fail、voxel_display ×3、debug_read_* 归并、writeback merge 硬门、score_sum 调查）；行为性改动已积累两轮，下轮先做一次 close-and-reopen 的 -e 验证再继续。
> - **轮 3（worker，git log 对账后开工）**：writeback merge 后再发射硬门 ✅——`ReportSchema.validate_report`（出口复验：未声明键/未请求时 diag 键重新引入/derived 键 → push_error，只刹车不修剪）+ VPG run_multi_asset 挂接 `gpu_autoobject_runtime_writeback` 前接线；现行 merge 逐键复核零误报（diag 键在场性合并、两侧同旗标）。--check-only ×2 过，产出留工作区待主循环收养。剩余真实可做仅 `debug_read_*` 散点开关归并（跨 settings 键 API 面，需甄别消费者，下轮评估拆法或建议转 defer）。
> - **轮 4（worker，终轮）**：`debug_read_*` 散点开关归并 → **裁决不做，转"确认存在但不建议做"**（数据流开关/昂贵回读 opt-in/自带门模式三种正交语义的有原则分离，归并=范畴错误或成本语义倒退；route 读开关对零外部设置者，详见 Debug 方案进度块轮 4 条）。可做清单至此耗尽（余项全部用户阻塞：probe rgba8 裁决、voxel_display GO 确认、恒定 provenance 键整删），**worker 循环按终止条件自停**（cron 76eca048 已删）。
> - **主循环对账+收养（同日，worker 循环开工前必读本行）**：轮 1/轮 2 产出已由主循环（提交型）独立验证（parse 全过 + 全新 -e 0 错 + writeback check ok=true + GOLDEN PASS 统一覆盖）并**收养提交**（`d35814b` / `3d8cf4b`）——轮 2 计划的"-e 验证"已完成，勿重做。⚠ 轮 1/2 的"待做队列"多数已完：score_sum=0.0 调查 ✅ `6c3be73`（根因结案+check 历史首次全绿）；R8/RGBA8 codec ✅ `415ac76`；probe rgba8 → 裁决报告已交付**待用户拍板勿动代码**；voxel_display ×3 → 设计稿 GO **待用户确认勿动代码**；spa_test dispose-fail → 已归"确认但不值得做"勿再做。真实可做剩余 = `debug_read_*` 散点开关归并、writeback merge 后再发射硬门（均低优先）。**协作约定：worker 循环产出留工作区+记录到本段即可，主循环负责统一验证与外科提交；开工前先 `git log --oneline -5` 与本段对账，避免重复劳动。**
>
> **追加（2026-07-12，用户点名 `scene_voxel_collision_field.gd` 定位审计）**：collision 与 color/complexity 同级化后对该文件全量调用图审计，三批处置完成——①纯删：复制自 tile-store 的死常量块（~38 行）、`import_mask_channel` 全链（含 `channel_import_mask.glsl`+`.import`、committer 转发，全仓零调用）、零调用的 `_make_sv_collision_field`、不可达 cylinder 分支（上游 `normalize_collision_samples` 已过滤非点采样）、4 个 VoxelGeneral 薄包装内联、`_volume["collision_field"]` 写沉井（5 写 0 读，含 committer 每次 rebuild 白算一次 GPU resample）；②点采样化：write-spec collision 记录改 CPU cell-map 直写（`> previous` 才更新，与旧 depth-1 盘 stamp(radius=0, compare_mode=0) 语义等价——`base_pixel_to_volume_pixel` 与旧 shader 同款 clamp），删 `_stamp_scalar_volume_disc`/`_ensure_volume_collision_layer`/`source_collision_field`（写一个体素的整场 GPU 上传/回读往返消除）；③归位：更名 `scene_voxel_field_builder.gd` / `SceneVoxelFieldBuilder`（对齐 committer 既有 `_field_builder` 叫法；通用 2D/3D stamp 原语 + occupancy + terrain-base + collision 摘要的真实定位），README/契约测试路径同步，shader 计数 44→43。inert shape/radius 元数据按 7/01 决定继续保留。
>
> **收尾（2026-07-12 下午，三代理审计核实 + 五代理执行）**：工作区积压批次全部提交——B2 footprint→常驻 `collision_records` 迁移 + ScoreTimingProfiler + 全仓 footprint 术语清扫（`d4ec6b7`）；collision-field 归位 + `_rebuild_sv` 广告键瘦身（`58e7a7b`，含预提交复核修出的 2 处瘦身回归：scene_voxel_tile_debug 计数/脏着色改走 committer API 与幸存 `dirty_scene_voxel_tiles` 快照、tile_store 2 孤儿函数删除）；命名方案文档删除（`76923e0`，词根法则存记忆）。
> **demo/test 长尾复核后执行完毕（`415ac76`）**：7 条 do_now 全落地——unshaded 材质 ×9 + 线框盒 ×2 → 新 `scripts/utils/demo_debug_visuals.gd`；`_scan_descriptor_paths`→`DemoAssets.discover_geo_files`；AutoAssetFactory `load_mesh`/`load_source_mesh`→DemoAssets 委托 + `resolve_source_mesh` 收编 `get_source_mesh`×2；R8/RGBA8 codec ×5→SceneVoxelTileCodec 委托；`score_voxel_tile.glsl` `make_invalid_eval_result()` 收编清零块 ×3；插件 `_make_flat_stylebox`/`_set_status_label`/`_scene_root_host` 三组。2 条复核后**转 defer**，先决准备已完成（2026-07-12 报告）：probe rgba8 ×2 →《probe-rgba8-语义分叉裁决报告.md》——核实为**潜伏语义债非现行 bug**（生产路径恒过 `normalize_probe`，分歧行仅手写非规范化字典可达；两版打包喂同一 shader，消费端对分歧不可观测），推荐裁决 = prefilter 四键条件为 canonical + 吸收 container 的 `pack_semantic(WHITE)` 回退（规范化数据字节零变化），**待用户拍板**；voxel_display MultiMesh ×3 →《voxel-display-multimesh-收敛设计稿.md》——`_build_writer_node` 共享构建器完整设计 + 6 条风险登记，结论 **GO（附条件：单文件单 commit、字符串/meta 逐字、-e 三路径目检）**，净省 ~25 行 + 9 处 dispose 守卫三连收敛为 3 处。spa_test dispose-fail ×45 **转"确认但不值得做"**（churn>收益）。

## 批次 0 — 疑似正确性 bug（已证伪，无需动作）

复核结论（2026-07-10 全链路追踪，详见 mem `redundancy-audit-2026-07-10`）：**无 bug，勿修**。

- `score_voxel_tile.glsl` 的稀疏路由 tile id 解码当前已是 canonical z-inner，与全部 producer/consumer 一致——审计原始行号已漂移失效。
- `reduce_scene_voxel_tile_summaries.glsl` 的 Y-inner 展开是 encode/decode **自洽**的独立路径（full-grid reduce 写 FRAME buffer，CPU 端 `_tile_coord_from_summary_index` 同为 Y-inner），与 resident 路径的 PERSISTENT buffer 从不共享槽位——即"双 tile-summary 有意分工"。
- `run_resident_placement_writeback_check` 根因不是此项，仍未查透。
- 教训：正确性改动必须追到 encode+decode 两端再判，单看 encode 差异会误报。

仍然成立的同族一致性隐患（批次 3 处理）：Y-yaw 旋转在 4 个 shader 重复实现且注释自述 "must match"。

## 批次 1 — 纯删除：死代码与孤儿文件

约 3,400 行 + 5.7MB，全部核实过项目全域零引用（含字符串派发、`.tscn`/`.tres`、tools、docs），行为零变化。

| 目标 | 规模 | 备注 |
| --- | --- | --- |
| 32 个孤儿 `.glsl` + sibling `.import` + 2 个源已删的 `.import` + `height_capture.gdshader` | ~1,690 行 | 含 `rotate_footprint_y.glsl`（顺带消除反号 yaw 约定隐患）；编辑器关闭时删 |
| `scripts/terrain_initializer.gd` 死的地形生成/程序化子树 + 3 个专属 shader | ~390 行 | |
| `scripts/auto_asset_factory.gd` 约 14 个死函数（过半文件） | ~210 行 | ⚠ 与文档联动，见下方拍板项 3 |
| `scripts/auto_object.gd`：CPU ray-pick 子系统（SPA 点击选择的旧平行实现）+ 8 个零调用方法 + no-op 元数据清理器 | ~220 行 | anchor 读取器删除有 `asset-semantic-probes.md:152` 文档背书 |
| committer 拆分疤痕：8 个死委托桩、tile_store/collision_field 源裁决管线遗留、`SCENE_VOXEL_TILE_*` 常量块三份、13 个死 preload | ~285 行 | keep-list 的 11 个 facade 桩不在此列 |
| `tools/shot_targetsv_brush_overlay.gd`（驱动已删的 demo API） | ~160 行 | |
| 长尾死符号：scene_voxel 小文件 statics、`target_scene_voxel_generator` 死 statics+死偏移常量、prefilter 死符号、`DemoAssets` 6 个零调用函数、`GodotComputeShaderBase` 的 `*_3d` 死链、`SPAEditorContract` 3 个 getter、runtime/container 零调用别名等 | ~350 行 | |
| 二进制孤儿：`baked_cliff_01/02.res`+`baked_leaf.res`（780KB）、4 个文档 `.png`（742KB，`.svg` 才被引用）、`textures/*.exr` ×6（2MB，运行时只读 `.raw`）、`landscape/TargetSV_Referebce.bgeo.sc`（2.2MB 拼写错的孤儿导出）、`Noot.md` | ~5.7MB | `geo/`、`*.hip` 为用户源资产，不动 |

### 待用户拍板的三项

均为"零调用但可能是有意保留的 API"（参照 committer facade 桩先例）：

1. `scripts/voxel_placement_output.gd` CPU 实例化/write-spec 家族（~350 行）——唯一活入口 `results_to_world_gpu`；删除方向与 resident-GPU-only 决定一致，但均为公共 statics。
2. `GPUAutoObjectRuntime` command-queue/ID-reservation 控制面（~550 行外部不可达）——`autoobject-gpu-runtime-architecture.md` 仍列为设计面。留 → 加 `# reserved design surface` 注释；删 → 同 commit 更新该文档。
3. `auto_asset_factory` 死函数 —— `auto-asset-scripting.md` 教程仍在讲解这些 API，且其示例路径（`cliff_03.FBX`、`flower.glb` 等）已全部悬空。教程若为活文档则函数不算死；否则代码与教程同批处理。

## 批次 2 — 共有逻辑提取（GDScript，约 900 行）

核实代理已给出精确边界（含"哪些别动"）：

- **resident target-read-buffer borrow 三连**在 `autoobject_probe_prefilter_gpu` 与 `voxel_placement_generator` 间重复（~110 行）：只抽 borrow 解析/校验核心为小 util；success/blocked/summary dict **留在各文件**——reason 后缀被契约测试逐字断言。
- **`meshfill_brush` 重实现 `TargetSVSetup` 的 TargetSV 引导显示栈**（同场景渲染，~60–90 行）：抽 fresnel/rim 材质工厂 + `DisplayChannel→VIEW_*` 映射为共享 statics；`voxel_to_world` 因地形源不同**不合并**。
- `scene_placement_actor.prepare_target_read_buffers_from_common_gpu` 同一 ~17 行失败 dict 重复 7 次（~90 行）→ 本地 `_fail(reason)` 工厂。
- `scene_voxel_tile_store` 两个 object_ref_update pass 共享 ~80 行结果组装；`gpu_autoobject_runtime` 两条 accepted-placement 路径重复 12-buffer uniform 组装 + 统计分支（~55 行）。
- `voxel_placement_writeback` 报告骨架 ×2、`_gpu_contract_blocked_minimal_output` 与 `_empty_prefilter_output` 仅差 3 键（各 ~35 行）；results→world dispatch 骨架在 output/writeback 重复（~40 行）。
- `meshfill_editor_plugin` 工具栏样板（状态标签构建/setter/宿主查找/teardown 重复 4–11 次，~60 行）+ StyleBoxFlat 改用 `*_all` setter。
- demo 侧收敛到已有 utils：unshaded 透明调试材质 5 处内联（SPA 已有工厂）、单位线框盒 ImmediateMesh ×2、`volume_score_demo._scan_descriptor_paths` → `DemoAssets.discover_geo_files`、`AutoAssetFactory.load_mesh/load_source_mesh` → `DemoAssets.load_mesh_info`、`get_source_mesh` 在 `AutoObject`/`AssetDescriptor` 重复、probe 打包与 pad-float-array ×3、`target_scene_voxel_generator` 重实现 codec 的 R8/RGBA8 帮助函数。
- 文件内重复：`spa_test` 50+ 处 dispose-then-fail 块（~45 行）、`voxel_display` 三次 MultiMesh 构建块、`score_voxel_tile.glsl` 内 EvalResult 16 字段清零 ×3。

## 批次 3 — GLSL 约定统一（可选）

⚠ **不要用 `#include` 共享**：本项目走 `shader_compile_spirv_from_source()` 直读原始字符串、绕过 RDShaderFile 预处理器，`#include` 根本不参与编译（且 RD `#include` 仅 depth-1）。共享方式 = **codegen 生成块**（从 GDScript 侧 SSOT 打印 GLSL 块，粘进 `.glsl` 并以 `// @@GEN…@@END` 标记进 git，见 mem: `refactor-stability-playbook`）。

1. 把 canonical z-inner 索引函数做成生成块，机械迁移各 shader 的手写副本。⚠ `reduce_scene_voxel_tile_summaries` 的 Y-inner 路径是 encode/decode 自洽的有意分工（见批次 0），**不迁**。
2. 同法统一 4 处 Y-yaw 旋转（`placement_results_to_world` 保留 yaw_degrees 缓冲契约，弧度就地换算）。
3. `pack_candidate_route_records_from_votes.glsl` 与 `expand_scene_voxel_tile_routes.glsl` 是 90% 拷贝对（~125 行），二选一收敛。

## 批次 4 — 配置与文档一致性

- `project.godot`：同一插件三个 enable 键（`meshfill_editor/enabled`、`MeshFillEditor/enabled` 均为陈旧遗留，canonical 为 `PackedStringArray` 形式）；无任何 C# 文件却有 `[dotnet]` 段。
- GLSL `local_size` ↔ GDScript 除数魔数双语言重复（如 `target_scene_voxel_generator` 的 8/4/8 对 `local_size_x=8,y=4,z=8`）——改用已有 `dispatch_groups_1d/2d/3d`，不再内联 `ceili(n/8.0)`。
- `scripts/terrain_config.gd:9` 注释引用不存在的 `TargetLandscape.bgeo.sc`；`auto-asset-scripting.md` 示例路径悬空（与批次 1 拍板项 3 合并处理）。

## 确认存在但不建议做

核实为真、判定收益低于成本/风险，**未来审计勿重报**（详情见 mem: `redundancy-audit-2026-07-10`）：

- GLSL unorm8/CAS/pack-unpack 帮助函数家族跨 15+ shader 重复——去重需 codegen 基建 + 全 shader 回归。
- `GPUAutoObjectRuntime` command-queue 内部的 4 函数同骨架重复——整个子系统 owner-gated（拍板项 2），先决后动。
- `ComputeKernel` 未用 API（`run`/`make_pass_indirect` 等）——新工具链预留面。
- 11 个 tools/*.gd CLI 入口"零引用"——命令行入口不算死。
- `TestUtils._collect_files_recursive` vs `DemoAssets.discover_geo_files`——语义不同。
- 双 tile-summary 实现（full-grid reduce vs dirty-range update 两套 8-word 布局）——有意分工。
- `debug_read_*` 散点开关归并（worker 轮 4 裁决）——"散点"实为三种正交语义的有原则分离：state-chain 数据流开关（默认值互相派生）、昂贵回读 opt-in（写侧 mask 同表达式焊死）、自带门模式（schema 已冻结"勿叠 diag 门"）；route 读开关对零外部设置者。归并 = 范畴错误或成本语义倒退，churn > 收益。
- `spa_test` 45 处 dispose-then-fail 块——测试文件不例行运行，收敛 churn>收益（2026-07-12 复核后归档）。
- `target_scene_voxel_generator` 的 5 个 codec 委托 shim——是 2026-07-12 去重产物**非纯转发别名**（其中 2 个被 `target_sv_loader` 外部调用），别名清扫勿删；可选后续=把 target_sv_loader 直指 codec 后内联其余 3 个。

## 执行与验证约定

- 每批一个外科式提交（只 stage 本批文件）；批内条目先按当前工作区复核再动手。
- 验证按 CLAUDE.md：关闭现有编辑器 → 修改 → 全新 `-e` 启动，console 干净 + 6800 桥应答即过；删 shader 必须在编辑器关闭时进行。
- 推荐顺序：三项拍板 → 批次 1 → 批次 2 → 批次 3/4；批次 2、3 可按 ≤32 subagent 上限分批委托。

## Open Questions

- ✅ ~~拍板项 1–3~~ 全部解决：1/3 随批次 1/2 消化；2 已拍板删除（`3fc9516`）。
- ✅ ~~`run_resident_placement_writeback_check` 根因~~（2026-07-12 桥驱动取证关闭，修复 `6c3be73`）：历史 `score_runtime_profile_binding_missing`/total_placed=0 签名已消失——最佳回溯=`9ede930` 修的 >128B push 静默清零（全零 push→candidate_count=0→工作组提前退出→contract 头永不写入），B2（`d4ec6b7`）补全常驻路径；残余失败是 check 自身的陈旧断言——penalty-only（`92ee35f`）下干净放置合法得 0 分而 check 仍断言 `score_sum>0`，即符号翻转 blast radius 漏掉的最后一个消费者。修法=VPG 转发 `placement_valid_count` + check 改断言 valid_count==total_placed（附正分腐坏守卫）。**check 历史首次全绿**（spawned=8 live=0→8 shader_consumed=true stats_applied=8）。
- 已知既有断链（非本方案批次引入，已挂后台任务，2026-07-12）：`tools/test_target_sv_point_cloud_conversion.gd` 调用已删的 `get_project_scene_voxel_snapshot` 与旧 `_sv` 广告键；`sv_anchor_collection_demo` 编辑器恢复时对树外节点 `get_path`/`look_at` 报错（SPA 并入进行中的时序缝）。
